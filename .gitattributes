# 统一换行。Windows 的 checkout 默认会把 LF 换成 CRLF，而启动器内置的
# docker-compose 副本是用 `include_str!` 编进二进制的、还要跟一个写死的 sha256 对账 ——
# 换行一变校验和就对不上（M2 的 CI 在 windows-latest 上实测撞到）。
* text=auto eol=lf

# 二进制，别碰
*.png  binary
*.jpg  binary
*.jpeg binary
*.ico  binary
*.icns binary
*.woff2 binary
