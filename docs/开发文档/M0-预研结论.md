# M0 · 预研结论与测试报告

> 里程碑：**M0 预研与仓库骨架**
> 执行时间：2026-09-19（上海时间）
> 开发机：34.133.47.117 · 测试机：hunter-test-01（34.133.8.3，Ubuntu 24.04 · 2 核 8G · Docker 29.8.1 + Compose v5.5.1）
> 网关：hermes（35.240.204.54，生产，本轮只读调用 + 签发一把测试 key）
>
> **本轮不写业务代码**，只做七项实测与仓库骨架。
> 所有数字都有命令与原始输出为证；测不到的一律写「未测」和原因，**不估算、不编造**。
> 与 [`docs/design/设计与技术方案.md`](../design/设计与技术方案.md) 冲突处**一律以本文为准**，
> 该文件开头已加一段差异索引指回这里。

---

## 摘要：对 M1–M4 影响最大的七条结论

| # | 结论 | 影响 |
|---|---|---|
| 1 | 网关对「格式错 / 不存在 / 已吊销 / 未带 key」返回**字节级完全相同**的 401 | 方案第 18 节 `E_KEY_INVALID` 的「区分原因」做不到。M2 改为本地判格式 + 401 一律当无效。已列入需上游配合 |
| 2 | 一把 `hunt_tools_` key **确实**同时走通大模型网关、数据网关、工具网关 | 「一把 key」的产品承诺成立，M2 可以按单 key 设计 |
| 3 | `docker compose --progress json pull` 的 `Downloading` 行带 `current`/`total`/`percent`（142/142 覆盖），`Extracting` 行**只有** `current` | M2 的进度条方案定了。解压阶段没有百分比，UI 要另作处理 |
| 4 | `docker pull` 在非 TTY 下**完全没有**逐层进度，只输出三行结果 | 不能按方案第 5.2 节「`docker pull` 逐镜像 + `--progress plain`」做 |
| 5 | 六个镜像合计 **810.6 MiB**（amd64 压缩），全部是 amd64+arm64 双架构 | 视觉稿写的「约 2.1 GB」要改成实测值 |
| 6 | 阿里云 ACR 与 Docker Hub 上**都没有**镜像，只有 GHCR 一个源 | 方案第 5.5 节的「三源测速」无源可测。需用户决定是否建国内源 |
| 7 | hunter-community 的 Release **没有任何 assets** | 方案第 5.2 节「从 Release 资产下载 compose」不成立，改走 raw.githubusercontent |

---

## 一、模型网关与 key 校验（方案 §8 / 附录 C）

### 1.1 结论

| 项 | 方案写的 | **实测** |
|---|---|---|
| 网关地址 | `https://llm.agentpit.io/v1` | `https://hunter.agentpit.io/api/saas/llm/v1` |
| 额度接口 | `POST /v1/keys/validate` | `GET /api/saas/llm/quota`（**没有** `keys/validate`） |
| 模型列表 | 含在 validate 响应里 | `GET /api/saas/llm/v1/models`（OpenAI 兼容） |
| key 前缀 | `hk_` | `hunt_tools_` |
| key 长度 | 未写 | **43 字符** = 前缀 11 + 随机 32（`saas_key.py` 的 `KEY_PREFIX + token_urlsafe(32)[:32]`） |
| 模型别名 | `hunter-default` / `hunter-deep` | **`hunter-chat`** / `hunter-deep` |
| 默认日额度 | 未写 | 300,000 token/天，**按 Asia/Shanghai 的 0 点**重置 |

`llm.agentpit.io`、`telemetry.agentpit.io`、`dl.agentpit.io` 三个域名**都不存在**（实测 NXDOMAIN）：

```
$ for d in telemetry.agentpit.io dl.agentpit.io llm.agentpit.io hunter.agentpit.io; do ... getent hosts $d ...
telemetry.agentpit.io        DNS 无记录 (NXDOMAIN)
dl.agentpit.io               DNS 无记录 (NXDOMAIN)
llm.agentpit.io              DNS 无记录 (NXDOMAIN)
hunter.agentpit.io           解析成功: 35.240.204.54   hunter.agentpit.io
```

### 1.2 有效 key 的两个接口（原始输出）

```
$ curl -H "Authorization: Bearer hunt_tools_****" https://hunter.agentpit.io/api/saas/llm/quota
{"ok":true,"used_today":0,"limit_daily":300000,"remaining":300000,"exhausted":false,
 "reset_at":"2026-09-20T00:00:00+08:00","reset_tz":"Asia/Shanghai","rate_per_min":20,
 "max_concurrency":4,"unit":"token（输入 + 含 thinking 的输出）","enabled":true,
 "global_exhausted":false}
HTTP 200 · 0.712s
```

```
$ curl -H "Authorization: Bearer hunt_tools_****" https://hunter.agentpit.io/api/saas/llm/v1/models
{"object":"list","data":[
  {"id":"hunter-chat","object":"model","created":1789814332,"owned_by":"huntercode",
   "description":"默认对话与工具调用 · 快、便宜","display_name":"Gemini 3.8 Flash",
   "upstream_model":"hunter-chat"},
  {"id":"hunter-deep","object":"model","created":1789814332,"owned_by":"huntercode",
   "description":"深度分析与长任务 · 更强但更贵，额度消耗快","display_name":"Gemini 3.1 Pro",
   "upstream_model":"hunter-deep"}]}
HTTP 200 · 0.680s
```

**给 M2 的字段映射**（视觉稿第 1 张的三张卡片）：

| 视觉稿上的卡片 | 数据来源 | 实测值 |
|---|---|---|
| 套餐 `Community` | ⚠️ **网关不返回 plan 字段** | 无来源。M2 改成显示「今日剩余」或去掉这张卡（见 §1.6 需用户决定） |
| 今日模型额度 `300,000 tokens` | `quota.limit_daily` | 300000 |
| 每日 00:00 重置 | `quota.reset_at` + `reset_tz` | `2026-09-20T00:00:00+08:00` · Asia/Shanghai |
| 可用模型 `2 个` | `models.data | length` | 2 |
| 模型名 | `models.data[].id` | `hunter-chat` · `hunter-deep`（**不是** `hunter-default`） |

`quota` 还额外给了 `rate_per_min: 20`、`max_concurrency: 4`、`exhausted`、`global_exhausted` —— 方案里没提，M2 可以用来做更准的错误提示。

### 1.3 ⚠️ 四类错误 key 的响应**完全相同**（本轮最重要的负面结论）

四种输入各调一次 `quota` 与 `models`：

| 输入 | HTTP | 响应体 |
|---|---|---|
| 格式错（`sk-totally-wrong-format-123`） | **401** | 见下 |
| 格式对但不存在（随机 `hunt_tools_` + 32 位） | **401** | **与上一行逐字节相同** |
| 完全不带 `Authorization` 头 | **401** | **相同** |
| 空 `Bearer` | **401** | **相同** |

统一的响应体（`/api/saas/llm/*`）：

```json
{"error":{"message":"这个地址是 HunterCode 的内置模型额度网关，需要一把 Hunter 平台 key 才能用。到 https://hunter.agentpit.io/dev/api-keys 免费申请（约 30 秒），把 hunt_tools_ 开头的那串填进「大模型 API Key」即可 —— 它和工具、数据源共用同一把，不需要第二把。也可以在设置里改用你自己的大模型 key（任何 OpenAI 兼容服务都行）。","type":"invalid_api_key","code":"hunter_key_required","param":null,"apply_url":"https://hunter.agentpit.io/dev/api-keys"}}
```

**「已吊销」没有实测**（会污染生产数据，红线 6 只允许新增一条 key 记录）。
但从 hermes 代码可以确定结果一样：`saas_key.verify()` 的 SQL 是
`WHERE key_hash = %s AND status = 'active'` —— 吊销的 key 查不到行，
与「不存在」走**同一个** `return None` 分支，因此响应必然也是同一个 401。

**对 M2 的影响**：方案第 18 节要求 `E_KEY_INVALID` 区分四种原因，**网关侧做不到**。M2 的实现定为：

1. **本地先判格式**：`key.starts_with("hunt_tools_") && key.len() == 43` —— 不满足就直接报「key 格式不对，应该是 `hunt_tools_` 开头的 43 位字符串」，**不发请求**；
2. 格式对、网关回 401 → 统一文案「这把 key 网关不认（可能填错了、或者已被吊销）」+ 申请链接；
3. **不能**假装区分出「已吊销」——那会是编造的诊断结论。

已列入 [待办池](待办池.md) **P0-1**，并在本文 §8「需上游配合」提出诉求。

### 1.4 一把 key 通吃三个网关（**产品承诺成立**）

同一把 key，三个网关各调一次：

| 网关 | 请求 | HTTP | 响应 |
|---|---|---|---|
| 大模型 | `GET /api/saas/llm/quota` | **200** | 见 §1.2 |
| 工具 | `GET /api/saas/verify` | **200** | `{"ok":true,"scopes":["tools"],"quota_daily":null,"used_today":0}` |
| 数据 | `GET /api/saas/data/api/v1/kline/600519?period=daily&limit=3` | **200** | 真实 K 线数组（见下） |

```
$ curl -H "Authorization: Bearer hunt_tools_****" \
    "https://hunter.agentpit.io/api/saas/data/api/v1/kline/600519?period=daily&limit=3"
[{"ts":"2024-09-18T16:00:00+00:00","open":1262.0,"high":1292.88,"low":1245.83,
  "close":1261.0,"volume":59988,"amount":7623739900.0}, ...]
HTTP 200 · 1.409s
```

数据网关是**路径白名单**制（`saas_data.py` 的 `_ALLOWED_PREFIXES`），不在白名单里的路径回 **403**：

```
$ curl ... https://hunter.agentpit.io/api/saas/data/health
{"error":"path_not_allowed","path":"health","message":"health 不在数据网关白名单里。"}
HTTP 403
```

白名单：`api/v1/news`、`api/v1/cninfo`、`api/v1/quote/`、`api/v1/kline/`、`api/v1/financial/`、
`api/v1/orderbook/`、`api/v1/money_flow/`、`api/v1/lhb/`、`api/v1/fund_holders/`、
`api/v1/governance/`、`api/v1/peers/`、`api/v1/research/`。

> 注意：数据网关的 401 体与大模型网关**不同结构**（扁平 `{"error":"hunter_key_required","message":...}`，
> 不是嵌套的 `{"error":{...}}`）。M2 的错误解析要按网关分别处理，不能共用一个反序列化结构体。

### 1.5 计量确实生效（真实消耗了一次额度）

发一次最小对话，再查 quota：

```
$ curl -X POST .../api/saas/llm/v1/chat/completions \
    -d '{"model":"hunter-chat","messages":[{"role":"user","content":"只回复一个字：好"}],"max_tokens":8}'
{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"好", ...}}], ...}
HTTP 200 · 4.156s

$ curl .../api/saas/llm/quota
{"ok":true,"used_today":115,"limit_daily":300000,"remaining":299885, ...}
```

`used_today` 从 **0 → 115**，计量真实有效。

模型名写错时回 **400**（这个**能**区分，与 401 不同）：

```json
{"error":{"message":"内置额度不支持模型「gpt-4o」。内置额度只提供两个模型：hunter-chat（…）、hunter-deep（…）。…","type":"invalid_request_error","code":"model_not_allowed","param":null,"allowed_models":["hunter-chat","hunter-deep"]}}
HTTP 400
```

### 1.6 额度用尽（402）**未实测**

要触发需要烧掉 30 万 token，本轮没做。从 hermes 代码（`saas_llm.py` 的 `_deny_to_response`
+ `llm_quota.py`）可确定：日额度用尽 → `code: "daily_quota_exceeded"`、
`type: "insufficient_quota"`、**HTTP 402**（不是方案里写的 429）；
`429` 是每分钟 20 次的限流，**只有 429 才带 `Retry-After`**。

已列入 [待办池](待办池.md) **P0-2**：M2 用 `llm_key_admin.sh --quota <id> 1` 把测试 key 额度
临时压到 1 token 触发真实 402，测完立刻改回。

---

## 二、测试 key 的签发（总控规则红线 6）

- **user_id**：`hangeaiagent`（hermes 的 `saas_key.user_id` 直接存这个字符串；同名用户此前已有 3 把测试 key，编号 42/44/45）
- **签发方式**：调 hermes 仓库自己的 `app.services.saas_key.generate("hangeaiagent", "launcher-m0")`，**只 INSERT 一行**，不改任何现有行
- **结果**：`id=49` · 前缀 `hunt_tools_q6sK` · 尾 4 位 `QMo2` · 43 字节 · 签发时间 `2026-09-19T10:38:11Z`
- **明文存放**：**只在**测试机 `~/.hunter-launcher-test/key`（`-rw------- 600`）。
  hermes 与开发机上的中转副本已 `shred -u` 删除并复查确认不存在。
- 本文、仓库、邮件、日志里一律只出现 `hunt_tools_****`

签发前后 `saas_key` 表：47 行 → 48 行（`active` 44 → 45），无 UPDATE、无 DELETE。

> 顺带说明：`verify()` 每次校验会 `UPDATE saas_key SET last_used_at = NOW()`。
> 这是 key 被正常使用的固有副作用，不是本轮的改动。

---

## 三、hunter-community 的 compose 与配置（方案 §7 / 附录 E）

来源：`agentpit-io/hunter-community` 最新 Release **`v1.2.0`**（发布于 2026-09-19T09:47:29Z）。

### 3.1 服务清单（六个服务，全部实测自运行中的栈）

| 服务 | 镜像 | 宿主端口 → 容器端口 | 健康检查 | 卷 |
|---|---|---|---|---|
| web | `ghcr.io/agentpit-io/hunter-community-web:1.2.0` | `3100 → 3000` | **镜像内置**：`curl -fsS http://127.0.0.1:3000`（30s/5s/start 15s/retry 3） | `hunter_secrets:ro` |
| api | `ghcr.io/agentpit-io/hunter-community-api:1.2.0` | `8100 → 8000` | **镜像内置**：`curl -fsS http://127.0.0.1:8000/api/health`（30s/5s/start 10s/retry 3） | `hunter_user_skills`、`hunter_packages`、`hunter_secrets` |
| opencode | `ghcr.io/agentpit-io/hunter-community-opencode:1.2.0` | `3921 → 3901` | **镜像内置**：`curl -fsS -u $OPENCODE_SERVER_USERNAME:$OPENCODE_SERVER_PASSWORD http://127.0.0.1:3901/` | `hunter_opencode_data`、`hunter_secrets:ro` |
| llm-shim | `ghcr.io/agentpit-io/hunter-community-llm-shim:1.2.0` | **不对外** | **镜像内置**：`python3 -c urllib.request.urlopen('http://127.0.0.1:3999/health')`（15s/5s/start 5s/retry 5） | — |
| postgres | `postgres:16-alpine` | `5442 → 5432` | **compose 里写的**：`pg_isready -U $POSTGRES_USER` | `hunter_pg_data` |
| redis | `redis:7-alpine` | `6479 → 6379` | **compose 里写的**：`redis-cli ping` | `hunter_redis_data` |

**关键发现：`docker-compose.yml` 里只有 postgres 和 redis 写了 `healthcheck:`，另外四个服务的健康检查烧在镜像里。**
所以启动器**不能**靠读 compose 文件判断「哪些服务有健康检查」，必须读 `docker compose ps --format json` 的 `Health` 字段。

证据（`docker inspect -f '{{json .Config.Healthcheck}}'`）：

```
hunter-community-api-1   {"Test":["CMD-SHELL","curl -fsS http://127.0.0.1:8000/api/health || exit 1"],"Interval":30000000000,"Timeout":5000000000,"StartPeriod":10000000000,"Retries":3}
hunter-community-web-1   {"Test":["CMD-SHELL","curl -fsS http://127.0.0.1:3000 || exit 1"], ...}
hunter-community-opencode-1 {"Test":["CMD-SHELL","curl -fsS -u ${OPENCODE_SERVER_USERNAME:-opencode}:${OPENCODE_SERVER_PASSWORD:-changeme} http://127.0.0.1:3901/ || exit 1"], ...}
hunter-community-llm-shim-1 {"Test":["CMD-SHELL","python3 -c \"import urllib.request;urllib.request.urlopen('http://127.0.0.1:3999/health',timeout=3)\""], ...}
```

**`/api/health` 存在**（api 镜像的健康检查就在打它），所以方案第 5.2 节「对 `localhost:3100/api/health` 做 HTTP 探测」可行 ——
但要注意那是 **api 的 8100**，不是 web 的 3100。

启动依赖顺序（compose `depends_on`）：`postgres`+`redis` healthy → `api` healthy + `llm-shim` healthy → `opencode`；`web` 只 `depends_on: api`（不等健康）。

### 3.2 ⚠️ Release 没有任何 assets

```
$ curl -sS https://api.github.com/repos/agentpit-io/hunter-community/releases/latest | jq '{tag_name, assets}'
{"tag_name": "v1.2.0", "assets": []}
```

方案第 5.2 节假设「从 Release 资产下载 `docker-compose.yml`」——**不成立**。

可行替代（已实测）：

```
$ curl -o /dev/null -w "%{http_code} %{size_download}B %{time_total}s\n" \
    https://raw.githubusercontent.com/agentpit-io/hunter-community/v1.2.0/docker-compose.yml
200 23990B 0.163s
```

**M3 的方案**：按 tag 从 raw.githubusercontent 取 + 随启动器内置一份同版本兜底（离线/被墙时用）。
已列入 [待办池](待办池.md) **P1-3**，同时向上游提「给 Release 挂 compose 资产」的建议（§8）。

### 3.3 启动器要生成的 `.env` 模板（草案，**已通过 `docker compose config` 验证**）

对照方案第 7 节改成真实值。`PLACEHOLDER_*` 由启动器在首次运行时生成。

```dotenv
# ── 版本与镜像源 ─────────────────────────────────────────
HUNTER_VERSION=1.2.0
HUNTER_REGISTRY=ghcr.io/agentpit-io

# ── 端口（被占用时由启动器自动 +1 改写，同时写进覆盖文件）──
WEB_HOST_PORT=3100
API_HOST_PORT=8100
OPENCODE_HOST_PORT=3921
POSTGRES_HOST_PORT=5442
REDIS_HOST_PORT=6479

# ── 数据库 ──────────────────────────────────────────────
POSTGRES_USER=hunter
POSTGRES_PASSWORD=<启动器生成的 24 字节随机 base64>
POSTGRES_DB=hunter

# ── 密钥 ────────────────────────────────────────────────
# JWT_SECRET 首次生成后**永不更换**（换了用户会掉登录、已存的 key 解不开）
JWT_SECRET=<启动器生成的 48 字节随机 base64>
# ⚠️ 留空！api 首启会自己生成并写进 hunter_secrets 卷，web/opencode 只读挂同一个卷。
#    .env.example 预填的 `hunter-internal-local` 是写在公开仓库里的公开字符串，
#    照抄等于没有认证（见待办池 P0-3）。
HUNTER_INTERNAL_KEY=

# ── 访问控制 ─────────────────────────────────────────────
# 启动器部署 = 本机自用，单用户模式开；端口也只绑 127.0.0.1（见覆盖文件）
HUNTER_SINGLE_USER=1
HUNTER_SETUP_TOKEN=

# ── 一把 key 走通全部链路（红线 2：本文件权限 600）───────────
HUNTER_API_KEY=<hunt_tools_ key>
DATA_SOURCE_PROVIDER=hunter

# ── 大模型：默认走 Hunter 内置额度网关 ──────────────────────
LLM_PROVIDER=openai_compat
LLM_BASE_URL=https://hunter.agentpit.io/api/saas/llm/v1
LLM_API_KEY=<同一把 hunt_tools_ key>
LLM_DEFAULT_MODEL=hunter-chat
LLM_SCHEMA_SANITIZE=0

# ── 深度分析 / 子智能体的模型名 ─────────────────────────────
# 来源：hunter-community 的 docs/builtin-llm/使用说明.md 第 197–208 行。
# 不写的话「对话能用、深度分析是坏的」。
ASSISTANT_MODEL_ROUTE=hunter-chat
ASSISTANT_MODEL_CHAT=hunter-chat
ASSISTANT_MODEL_COMPRESS=hunter-chat
AGENT_MODEL_ROUTER=hunter-chat
AGENT_MODEL_ROUTE_LITE=hunter-chat
AGENT_SUB_WL_MODEL=hunter-chat
AGENT_SUB_PORT_MODEL=hunter-chat
AGENT_SUB_EVENT_MODEL=hunter-chat
SIGNAL_ANALYSIS_MODEL=hunter-chat
AGENT_SUB_RESEARCH_MODEL=hunter-deep

# ── opencode Basic auth ─────────────────────────────────
OPENCODE_USER=opencode
OPENCODE_PASS=<启动器生成的随机值>
```

**与方案第 7 节的差异**：方案写的 `OPENCODE_REGISTRY` / `OPENCODE_TAG` / `HUNTER_LAUNCHER` /
`HUNTER_INSTALL_ID` 这四个变量，在 hunter-community 的 compose 与 `.env.example` 里**都不存在**，
写进去不会有任何效果。镜像源与 tag 的真实变量是 `HUNTER_REGISTRY` / `HUNTER_VERSION`。
`HUNTER_LAUNCHER=1`（方案第 13 节让 api 开放 `/api/system/*`）也需要上游配合才有意义，见 §8。

### 3.4 启动器要生成的覆盖文件（草案，**已验证**）

红线 4：除 web 外的端口一律绑 `127.0.0.1`。

```yaml
# ~/.hunter/app/docker-compose.launcher.yml
# 由 Hunter 启动器生成 · 请勿手改（改了下次启动会被覆盖）
# 作用：1) 把除 web 外的端口全部收回 127.0.0.1  2) 落实端口冲突改写后的值
services:
  web:
    ports: !override ["${WEB_HOST_PORT:-3100}:3000"]
  api:
    ports: !override ["127.0.0.1:${API_HOST_PORT:-8100}:8000"]
  opencode:
    ports: !override ["127.0.0.1:${OPENCODE_HOST_PORT:-3921}:3901"]
  postgres:
    ports: !override ["127.0.0.1:${POSTGRES_HOST_PORT:-5442}:5432"]
  redis:
    ports: !override ["127.0.0.1:${REDIS_HOST_PORT:-6479}:6379"]
```

`!override` 标签是必须的：compose 默认对 `ports` 做**追加**合并，不加这个标签会变成
「既监听 0.0.0.0 又监听 127.0.0.1」，红线 4 就白写了。

验证（测试机，端口改成 3101/8101/3922/5443/6480 以避开旧栈）：

```
$ docker compose -p hunter -f docker-compose.yml -f docker-compose.launcher.yml config
退出码 0 · 解析结果 239 行 · stderr 为空

$ ... config --format json | jq '.services | ... ports'
api:       127.0.0.1:8101->8000
llm-shim:  (不对外)
opencode:  127.0.0.1:3922->3901
postgres:  127.0.0.1:5443->5432
redis:     127.0.0.1:6480->6379
web:       0.0.0.0:3101->3000        ← 只有 web 对外，红线 4 达成
```

env 注入也正确：

```json
{"LLM_BASE_URL":"https://hunter.agentpit.io/api/saas/llm/v1","LLM_DEFAULT_MODEL":"hunter-chat",
 "LLM_SCHEMA_SANITIZE":"0","DATA_SOURCE_PROVIDER":"hunter","HUNTER_SINGLE_USER":"1",
 "HUNTER_INTERNAL_KEY":"","AGENT_SUB_RESEARCH_MODEL":"hunter-deep","LLM_API_KEY":"hunt_tools_****"}
```

**项目隔离已验证**：`docker compose -p hunter` **能**盖掉 compose 文件里写死的 `name: hunter-community`
（`config --format json | jq .name` 输出 `hunter`），因此启动器的卷会叫 `hunter_hunter_pg_data`，
与旧栈的 `hunter-community_hunter_pg_data` 互不干扰。

### 3.5 端口冲突：测试机现状正好是测试素材

```
3100 已占用   8100 已占用   3921 已占用   5442 已占用   6479 已占用
3101 空闲     8101 空闲
```

旧栈把五个默认端口全占了 —— M3 的「端口冲突自动改端口」可以直接在这台机器上真实验证，不用造场景。

---

## 四、镜像：大小、架构、镜像源（方案 §9）

全部用 `docker manifest inspect` 读 manifest，**没有拉取任何镜像**。

### 4.1 GHCR · 全部双架构 · 压缩后合计 810.6 MiB

| 镜像 | 多架构 | linux/amd64 压缩 | linux/arm64 压缩 | 层数 |
|---|---|---|---|---|
| `hunter-community-web:1.2.0` | ✅ amd64 + arm64 | **315.9 MiB**（331,344,498 B） | 309.4 MiB | 12 |
| `hunter-community-api:1.2.0` | ✅ | **204.7 MiB**（214,680,985 B） | 204.3 MiB | 12 |
| `hunter-community-opencode:1.2.0` | ✅ | **145.8 MiB**（152,979,569 B） | 146.6 MiB | 18 |
| `hunter-community-llm-shim:1.2.0` | ✅ | **17.2 MiB**（18,054,459 B） | 17.6 MiB | 5 |
| `postgres:16-alpine` | ✅ | **110.6 MiB** | 108.6 MiB | — |
| `redis:7-alpine` | ✅ | **15.4 MiB** | 15.9 MiB | — |
| **合计** | | **810.6 MiB** | 802.4 MiB | |

四个自家镜像都是 OCI image index（`application/vnd.oci.image.index.v1+json`），
除 amd64/arm64 外还各有两个 `unknown/unknown` 条目（buildx 的 attestation/SBOM manifest，838 B，不是可运行镜像 —— 解析时要按 `platform.os == "linux"` 过滤掉）。

**视觉稿第 2 张写的「约 2.1 GB」与实测差 2.6 倍。** 那个数字应该是**解压后**的磁盘占用
（旧栈本地 web 镜像单个就约 1.68 GB）。用户等的是**下载**，所以进度与预估都该用压缩大小。
M2 的文案按实测的 ~810 MiB 写，并可以另给一行「解压后约占磁盘 X GB」。

Apple Silicon 不需要担心：四个自家镜像都有原生 arm64，方案第 6 节「镜像必须双架构发布」**已经满足**。

### 4.2 ⚠️ 阿里云 ACR 与 Docker Hub 上都没有镜像

对 16 个候选引用逐个 `docker manifest inspect`（含 `hunter-community-*` 与 `hunter-*` 两种命名）：

```
registry.cn-hangzhou.aliyuncs.com/agentpit/hunter-community-web:1.2.0      6972ms  errors: denied: requested access to the resource is denied
registry.cn-hangzhou.aliyuncs.com/agentpit/hunter-web:1.2.0                5656ms  errors: denied: ...
docker.io/agentpit/hunter-community-web:1.2.0                              747ms   errors: denied: ...
docker.io/agentpit/hunter-web:1.2.0                                        710ms   errors: denied: ...
（其余 12 条同样全部 denied）
```

`denied` 无法区分「仓库不存在」与「存在但私有」，但**结论一样：启动器用不了**。

**从测试机到三个源的 manifest 请求耗时**：

| 源 | 耗时 | 备注 |
|---|---|---|
| **GHCR** | 1,993 – 2,272 ms | 唯一可用 |
| Docker Hub（`library/*`，可用） | 5,556 – 5,630 ms | 只有 postgres/redis 走这里 |
| Docker Hub（`agentpit/*`，denied） | 670 – 864 ms | 快是因为直接被拒，不是真可用 |
| 阿里云 ACR（denied） | 5,656 – 8,624 ms | — |

> 测试机在美国爱荷华（GCP us-central1），这组耗时**不代表国内用户的体感**，不能拿去做「国内源更快」的结论。

**对 M2 的影响**：方案第 5.5 节的「三源测速 + 国内默认 ACR」**无源可测**。M2 实现为：

- 默认且唯一的源：`ghcr.io/agentpit-io`
- 设置页允许用户**手填**自定义 registry（写进 `HUNTER_REGISTRY`），给自建镜像仓库的内网用户用
- **不做**假的「测速」界面 —— 一个源没什么好测的（红线 1）
- 视觉稿第 2 张的「已自动选择最快的镜像源（阿里云 · 杭州）」这句文案要改掉

已列入 [待办池](待办池.md) **P1-2**，并在 §9「需要用户决定的事」提出。

---

## 五、Docker 进度输出与状态解析（方案 §5.2）

测试机：Docker **29.8.1**（containerd 快照器 + overlayfs）· Compose **v5.5.1**。
测试方法：拉一个本机从未有过的镜像（`golang:1.22-bookworm`）保证是**冷拉**。

> 踩过的坑：`docker rmi` 在 containerd 镜像存储下不清内容存储，重拉是秒级且**不产生任何进度事件**。
> 第一次试验就是因为这个得出了「compose json 只有 2 行」的错误结论，换成从未拉过的镜像才拿到真实数据。

### 5.1 `docker compose --progress json pull` —— **选定方案**

`--progress` 的合法取值：`auto, tty, plain, json, quiet`。

冷拉一个 7 层镜像共 **457 行** JSON，字段全集：
`id` · `parent_id` · `status` · `text` · `details` · `current` · `total` · `percent`

| `text` | 行数 | 带 `current` | 带 `total` | 带 `percent` |
|---|---|---|---|---|
| `Pulling` / `Pulled`（镜像级） | 1 / 1 | — | — | — |
| `Pulling fs layer` | 7 | — | — | — |
| `Downloading` | **142** | **142** | **142** | **142** |
| `Download complete` | 8 | — | — | — |
| `Extracting` | **290** | 290 | **0** | 0 |
| `Pull complete` | 7 | — | — | ✅ 100 |
| `Already exists` | 1 | — | — | — |

真实行样例：

```json
{"id":"Image golang:1.22-bookworm","status":"Working","text":"Pulling"}
{"id":"4f4fb700ef54","parent_id":"Image golang:1.22-bookworm","status":"Working","text":"Pulling fs layer","details":"0B"}
{"id":"35af2a7690f2","parent_id":"Image golang:1.22-bookworm","status":"Working","text":"Downloading","details":"1.049MB","current":1048576,"total":64394292,"percent":1}
{"id":"a492eee5e559","parent_id":"Image golang:1.22-bookworm","status":"Working","text":"Downloading","details":"42.99MB","current":42991616,"total":48479687,"percent":88}
{"id":"a492eee5e559","parent_id":"Image golang:1.22-bookworm","status":"Working","text":"Extracting","details":"1B","current":1}
{"id":"1451027d3c0e","parent_id":"Image golang:1.22-bookworm","status":"Done","text":"Pull complete","details":"0B","percent":100}
{"id":"Image golang:1.22-bookworm","status":"Done","text":"Pulled"}
```

**这一条满足视觉稿第 2 张的全部需要**：`parent_id` 就是 `"Image <完整镜像引用>"`，
按它分组即可得到每个镜像一条进度条；层级 `current`/`total` 求和得到每镜像百分比；
所有镜像求和得到总百分比与「1.36 GB / 2.12 GB」那一行。

**M2 的解析方案**（定稿）：

```
docker compose -p hunter -f docker-compose.yml -f docker-compose.launcher.yml \
  --progress json pull
```

- 逐行读 stdout，`serde_json` 解析，**解析失败的行直接丢弃不报错**（未来 compose 加新行型不会炸）
- 按 `parent_id` 分组到镜像；维护 `HashMap<layer_id, (current, total)>`
- 每镜像百分比 = `Σcurrent / Σtotal`；**总大小从 manifest 预先算好**（§4.1 已证明可读），
  这样进度条从第一秒就有正确的分母，不用等所有层的 `total` 都到齐
- `text == "Pull complete"` → 该层记为 100%；`text == "Pulled"` → 该镜像完成
- `--progress` 是 **`docker compose` 的全局参数**，要写在 `pull` 前面

### 5.2 ⚠️ `docker pull` 在非 TTY 下完全没有进度

```
$ docker pull traefik:v2.10          # stdout 不是终端时
v2.10: Pulling from library/traefik
Digest: sha256:6341b98aec5ec8969b99e9730f4c5b22cca3103d42b6bbe4b7a3c910758fe565
Status: Downloaded newer image for traefik:v2.10
docker.io/library/traefik:v2.10
```

就这四行，**没有任何逐层信息**。`docker pull` 也**没有** `--progress` 参数
（`docker pull --help` 只有 `--platform` 和 `-q/--quiet`；`--progress` 是 `docker compose` 独有的）。
启动器是拿管道读子进程 stdout 的，必然是非 TTY —— 所以方案第 5.2 节的
「`docker pull` 逐镜像 + `--progress plain`」这条路**走不通**，除非在 Rust 侧分配伪终端再解析 ANSI，没必要。

作为对照，Engine API `POST /images/create` 的 JSON 流确实带精确字节
（`{"id":"7c897fde901f","status":"Downloading","progressDetail":{"current":1048576,"total":248755360}}`），
但它要求直连 docker socket / Windows 命名管道，三平台路径各不相同 ——
正是方案第 2 节选 CLI 的原因。**既然 compose 的 json 进度已经够用，就不引入这条依赖。**

其余两种格式供参考：`--progress plain` 是人读的文本行（`" 35af2a7690f2 Downloading 15.73MB"`，
**没有分母**，解析不出百分比）；`--progress quiet` 是**零输出**。

`docker compose pull` 自己的选项：`--dry-run`、`--ignore-buildable`、`--ignore-pull-failures`、
`--include-deps`、`--policy missing|always`、`-q/--quiet`。
其中 **`--ignore-pull-failures`** 对 M2 的「失败重试/换源」有用：可以先尽量拉，再针对失败的单独重试。

### 5.3 `docker compose ps --format json` 的字段

每个容器一行 JSON（**不是**一个 JSON 数组）。完整字段：

`Command` · `CreatedAt` · `Engine` · `ExitCode` · **`Health`** · `ID` · `Image` · `Labels` ·
`LocalVolumes` · `Mounts` · `Name` · `Names` · `Networks` · `Ports` · `Project` · **`Publishers`** ·
`RunningFor` · **`Service`** · `Size` · **`State`** · `Status`

启动器要用的（运行面板 + 启动轮询）：

```json
{"Name":"hunter-community-api-1","Service":"api","State":"running","Health":"healthy","ExitCode":0,
 "Publishers":[{"TargetPort":8000,"PublishedPort":8100,"URL":"0.0.0.0"},
               {"TargetPort":8000,"PublishedPort":8100,"URL":"::"}]}
```

实测六个服务全部 `State: running` / `Health: healthy`。

三个坑：

1. **`Publishers` 每个端口有两条**（IPv4 `0.0.0.0` 和 IPv6 `::`）。运行面板显示端口时要去重，否则视觉稿第 3 张的服务网格会出现重复端口。
2. **没有容器时 stdout 是空字符串，退出码仍是 0**。解析器要把空输入当成「还没起」，不能当错误（[待办池](待办池.md) P1-6）。
3. `llm-shim` 不发布端口，它的 `Publishers` 是 `[{"TargetPort":3999,"PublishedPort":0,"URL":""}]` —— `PublishedPort: 0` 要显示成「内部」而不是端口 0。

### 5.4 Docker / daemon 检测（方案 §5.1）

| 场景 | 命令 | 退出码 | stdout | stderr |
|---|---|---|---|---|
| 一切正常 | `docker version --format json` | **0** | `{"Client":{...},"Server":{"Version":"29.8.1",...}}` | — |
| **daemon 没起** | 同上 | **1** | `{"Client":{...},"Server":null}` ← **Client 段仍在** | `failed to connect to the docker API at unix://...; check if the path is correct and if the daemon is running: ...` |
| 没装 docker | 同上 | — | — | 进程起不来（`ENOENT`） |

**判定逻辑（M2 定稿）**：进程起不来 → `E_DOCKER_MISSING`；
退出码非 0 但 stdout 能解析出 `Client` 且 `Server` 为 `null` → `E_DAEMON_DOWN`；
退出码 0 → 读 `Server.Version` 比最低版本。
**关键：不能只看退出码**，退出码 1 的两种含义靠 `Server == null` 区分。

compose 插件：`docker compose version --format json` → `{"version":"v5.5.1"}`。
daemon 不可达时 `docker compose ps` 退出码 1，stderr 同上。

---
