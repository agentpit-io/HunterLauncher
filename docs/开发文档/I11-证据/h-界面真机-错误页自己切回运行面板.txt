# U1 / U3 · 真界面（Xvfb + 真 Docker + 真 key，不是演示模式）
# 测试机 34.133.8.3，2026-09-23 03:48–03:56（UTC 时间戳来自机器；上海时间 +8）

## 现场怎么造的（把 0.1.9 那一幕在 Linux 上重演）
* 六个容器一直健康地跑着（hunter 项目，端口 3101 / 8101 / 3922 / 5443 / 6480）
* 把 ~/.hunter-i10gui/app/docker-compose.yml **挪走**，并把 launcher.toml 的 tag 改成
  一个不存在的版本 9.9.9 —— 于是安装会在「挑下载源」那一步快速失败
* install.done 改回 false，让界面重走向导

## 过程（截图 06 / 07，逐张见 docs/screenshots/I11 之外的会话截图说明）
03:49:00  开工前复查：这台机器上还没有装过 Hunter（264 毫秒）   ← 复查说的是实话：compose 文件确实不在
03:49:19  AI 自动安装失败（E_PULL_FAILED）：9.9.9 这个版本在所有候选镜像源上都拉不到
03:50:47  自动续装断路器到头，界面停在「出错了」

界面上这时的样子（截图 06-error.png）：
  出错了 · 镜像拉取失败 · E_PULL_FAILED
  ┌ 现在是什么情况 [实测] ─────────────────────────────┐
  │ 这台机器上还没有装过 Hunter                        │
  │ 服务 0 / 6 就绪 · 网页这一次没应答                  │
  │ [重新查一次] [看逐条证据]   这一块每 20 秒自己重查一次… │
  └────────────────────────────────────────────────┘
  底部按钮：[查看日志]                 [上一步] [关闭] [重试(主)]

## 外部修好，界面这边不点任何按钮
19:53:01 (UTC)  把 docker-compose.yml 放回去（六个容器全程一直是 healthy）
19:53:31 (UTC)  截图：界面**已经自己回到运行面板**，顶上一条绿色横幅：
                「Hunter 已经在正常运行」
                「Hunter 已经在正常运行（6 / 6 健康，网页返回 HTTP 200）」
                「刚才那个错误已经不成立了 —— 服务是在外部被修好的，也可能是启动器自己修好的。
                  没有重新安装，也没有重复下载。」
→ 从外部修好到界面跟上，**≤ 30 秒，零点击**。

## U3 ·「关闭」的语义（同一次真机会话）
点错误页的「关闭」之后：
  $ xwininfo -id <窗口>  →  Map State: IsUnMapped     ← 窗口收起来了
  $ ps -p <pid>          →  进程还在（etime 04:21）     ← 没退出
  $ docker ps --filter label=com.docker.compose.project=hunter
    六个容器全部 Up ... (healthy)                        ← 服务一个都没动
