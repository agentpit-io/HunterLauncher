# HunterLauncher · 总进度表

> 时间一律**上海时间**。状态只有四种：`未开始` / `进行中` / `完成` / `失败`。
> 「完成」= 代码合进 main + 在测试机上真实跑通 + 有截图 + 成果文档已写。
> 单项做完但没通过测试的，状态仍是「进行中」。

最后更新：2026-09-24（I14 收尾，发布 0.1.14 之后）

---

## 一、里程碑

| 里程碑 | 目标 | 交付物 | 状态 | 完成时间 |
|---|---|---|---|---|
| **M0** | 预研与仓库骨架 | 网关/镜像/Docker/Tauri 七项实测；README、`.gitignore`、`docs/`、总进度表、待办池 | **完成** | 2026-09-19 |
| **M1** | Tauri 骨架 + 设计令牌 + 向导壳 + CI | 三平台可编译；深海军蓝 + 琥珀金设计令牌；13 个页面（向导 7 + 运行面板 + 设置 / 日志 / 反馈 / 错误页）；状态机纯函数 + 47 个单测；`ipc.ts` 与演示数据模式；本地打包字体；GitHub Actions CI | **完成** | 2026-09-19 |
| **M2** | Rust 核心 + 真实向导全流程 + headless | `runtime::docker`、`config`、`gateway`、`registry`、`compose`（`--progress json` 流式解析）、`flow` 编排、`--headless`；前端接真实 IPC 与事件流；106 个 Rust 单测 | **完成** | 2026-09-20 |
| **M3** | 运行面板 + 托盘 + 设置 + 反馈诊断 + Linux 安装包 | 运行面板接真实数据（含「数据源」卡）；托盘 11 项 + 状态随服务变化 + 退出询问两条分支 + 三平台开机自启；设置页（镜像源手动固定、**模型模式切换并重建容器**、遥测开关与队列原文）；日志页按服务/实时/导出；反馈分节预览 + 脱敏 + zip + 预填 issue；本地遥测队列；`.deb`/`.AppImage` 真装真卸；149 个 Rust 单测 | **完成** | 2026-09-20 |
| **M4** | 发布流水线 + 自更新 + 升级备份 | 三平台 `release.yml`（未签名包 + prerelease + latest.json + COS 同步）、`cn-mirror.yml`；启动器自更新（minisign 验签，`.deb` 走「下载 + 一条命令」）；Hunter 升级（`pg_dump` 备份 + 失败自动回滚）；离线包导入/导出；README 下载与放行图文、使用说明 / 开发指南 / 常见问题 / CHANGELOG；`launcher-v0.1.0`（仍标 prerelease） | **完成** | 2026-09-20 |

## 二、迭代轮

| 轮次 | 主题 | 取活来源 | 状态 | 完成时间 |
|---|---|---|---|---|
| **I1** | 从待办池取 P0 + 全量回归 + 自审 | [待办池.md](待办池.md) | **完成** | 2026-09-20 |
| **I2** | 全量回归 + 自审 + 从待办池取 P1 | [待办池.md](待办池.md) | **完成** | 2026-09-20 |
| **I3** | 全量回归 + 自审 + 从待办池取 P1/P2 + 收尾 | [待办池.md](待办池.md) | **完成** | 2026-09-20 |
| **I4** | **macOS 真机首测暴露的 P0** + 向导顺序调整 + **AI 诊断助手** | 用户 Mac 真机反馈 + 本轮需求 | **完成** | 2026-09-20 |
| **I5** | **macOS 端口探测 P0**（`SO_REUSEADDR` 把被占端口判成空闲）+ **AI 自动驾驶安装（多智能体）** | 用户 Mac 真机跑 0.1.4 + `plan/AI自动驾驶安装_多智能体方案.md` | **完成**（macOS 真机待用户本机验收） | 2026-09-21 |
| **I6** | **macOS 子进程 PATH P0**（docker 找不到自己的凭据助手，公开镜像都拉不动）+ 原话不再丢 + 防来回换源 | 用户 Mac 真机跑 0.1.5（`plan/mac-auto-0.1.5.log`） | **完成**（macOS 真机待用户本机验收） | 2026-09-21 |
| **I7** | **复核员**（第二个模型）+ **`install_runtime`**（macOS 上零点击装一套完全用户态的容器运行时）+ **`reuse_existing_hunter`**（直接用你已经装好的那一套）+ 一键反馈直达 + **免费版只允许本机访问**（六个服务端口全部绑 `127.0.0.1`，手改配置也放不开；待办池 P1-20 关闭） | 设计文档 §十分期里原定 I6 的四项（待办池 P1-26）+ 用户 2026-09-21 19:05 的决定 | **完成**（macOS 那条链路待用户本机验收） | 2026-09-21 |
| **I8** | **绝不让用户自己动手**（界面上不再出现「请在终端里执行 …」与「我执行完了，继续」）+ **全自动**（安装过程中不要用户做任何决策与点击）+ 0.1.7 真机首测的两个根因 + 协议改 Apache-2.0 | 用户 2026-09-21 22:10 / 22:35 两条决定 + 用户 Mac 真机跑 0.1.7 | **完成** | 2026-09-21 |
| **I10** | **0.1.9 真机：内置虚拟机没有 DNS** —— 启动器自带的 Ubuntu 24.04 minimal 镜像里没有 systemd-resolved，`/etc/resolv.conf` 出厂就是断链，虚拟机与它里面**每一个容器**都解析不了域名；表现是 opencode 健康检查超时（`E_START_TIMEOUT` · 规则层 unknown），真正致命的是容器连不上模型网关。治本（启动器自己在那台虚拟机里把 resolv.conf 写好 + 开机重写的 systemd 服务）+ 把「容器能不能上网」升成必检项（两个新错误码）+ 已装好的机器自己痊愈 | 用户 Mac 真机跑 0.1.9（`plan/mac-0.1.9-20260922/`） | **完成**（macOS 真机待用户本机验收） | 2026-09-22 |
| **I11** | **已经装好了就别再装**（0.1.9 真机那次失败之后的 71 分钟）：错误页会自己低频复查现状、查到六个服务全就绪 + 网页真的应答就**自己回到运行面板**；「重试」第一步改成只读探测，已经在跑就什么都不做、只有个别服务不正常就只动那几个（`--no-recreate`，健康的容器一个不重建）、真没装才走完整安装；错误页与诊断卡片新增「关闭」（回托盘，不退出、不停容器）；诊断的端口一节分三类（0.1.9 那份把五个自己占着的端口全写成了「空闲」）；`.env` 里写上 `COMPOSE_PROJECT_NAME` / `COMPOSE_FILE`，手工敲 `docker compose` 也带覆盖文件，外加「跑着的容器绑在哪 vs 配置说该绑哪」的开机自查与自动收回 | 用户 2026-09-22 22:50 的原话 + `plan/mac-0.1.9-20260922/` | **完成**（macOS 真机待用户本机验收） | 2026-09-23 |
| **I12** | **装好了就记住**（0.1.9 那次现场的最后一段）：打开启动器先只读查一次现状再决定进哪一页（**判据从「只看 `install.done` 标记」改成「现状优先」**，六个容器在就是装过、标记假了自己补上），`[install]` 记全「哪一版启动器装的 / 哪一版 Hunter / 用的哪套运行时 / 上一次好好的是什么时候」；运行面板上半部新增**三层资源**（这台电脑 / Hunter 运行环境 / 数据卷与镜像 + 每个服务的 CPU 内存，窗口看不见就停采）；停止可顺带停运行环境（释放约 4 GB 内存）、启动先把运行环境弄好、可只重启一个服务，托盘三项同步；**电脑或运行环境重启后容器自己回来**（覆盖文件写死重启策略 + 启动器起来发现「运行环境在跑、容器没起」就自动拉起，但绕开用户自己点过的停止）；**重装前只读检测已有数据**（数据库 + 密钥卷 + `JWT_SECRET` 都在就直接沿用，不重建；唯一拦下来的是「数据比镜像新」，新错误码 `E_DATA_DOWNGRADE`） | 用户 2026-09-22 23:10 真机截图 + `plan/运行管理_备份恢复与卸载_需求与技术方案.md` R1/R2/R3/R5 | **完成**（macOS 真机待用户本机验收） | 2026-09-23 |
| **I13** | **数据有了退路，删除有了门槛**（用户 2026-09-22 原话里剩下的三件事）：**R6 备份与恢复** —— `pg_dump -Fc` + 密钥卷 / 自建技能 / 会话记录打包 + `.env` 与两份 compose + `meta.json`（逐文件 sha256、**真数出来的**表数行数），**做完立刻用 `pg_restore --list` 校验，读不出目录就判这次失败**；默认每晚 00:00、保留最近 3 天（按天保留，升级前备份另算留最近 2 份）、目录按平台自动建议且一定在 `~/.hunter` 之外；三平台定时任务（macOS 用户级 LaunchAgent / Linux `systemd --user` + `Persistent=true` / Windows 任务计划 + `StartWhenAvailable`），**启动器关着也跑、一条 `sudo` 都没有、删除应用时一并移除**；Hunter 停着时只临时起 postgres 做完停回去，运行环境没起则记为「跳过」不擅自拉起整套；恢复是六步（先给当前数据备份 → 停 api/opencode/web/llm-shim → `pg_restore --clean --if-exists` → 恢复密钥卷与 `JWT_SECRET` → 起回来 → 等健康），支持**从任意目录的备份恢复**（换电脑迁移）。**R4 删除应用** —— 三步（选范围，默认「只删应用、保留数据」→ **逐字输入**「删除应用」/「删除应用和数据」→ 执行并逐条报告释放空间）；「保留数据」那一档下「顺便删运行环境」**禁用**（数据卷就在那台虚拟机的磁盘里）；`.env` 与备份目录永远不删（前者一丢，留下来的数据库就再也连不上，待办池 P1-29 那个死局）。**R7 异常监测** —— 七类监测项、两级阈值（全在 `[monitor]`）、结论 + 一键处理（**只清本项目旧镜像、悬空卷、过期备份**，每一样都先问「有没有容器在用它」），规则层判不了的把**脱敏指标快照**交给诊断助手；「重启了几次」看的是窗口内的**差值**不是 `RestartCount` 累计值（跨进程状态文件）；同一问题 24 小时内不重复。**守卫三道新窄门**（`argv_schedule` / `own_volume` + `argv_volume_rm` / `argv_image_rm`），**通用那道门一个字都没放松** | 用户 2026-09-22 原话 + `plan/运行管理_备份恢复与卸载_需求与技术方案.md` R4/R6/R7 | **完成**（macOS 的 LaunchAgent 那条路待用户本机验收） | 2026-09-23 |
| **I14** | **0.1.13 在用户 Mac 上真跑了一遍，四处不对**：**F1（P0）macOS 上自更新完全不可用** —— `--self-update` 在 mac 上下的是 Linux 的 `.deb`，然后 `dpkg: command not found`（`match` 只单独处理了 AppImage，macOS / Windows 全落进「下 .deb」那个兜底分支）。改成**按 `latest.json` 里这台机器自己那一项取包**（`fetch_asset` + `target_key`），macOS 解 `*_universal.app.tar.gz` 后「先把旧的挪开、再把新的搬进来」两次 `rename` **就地换掉那个 `.app`**（写不写得了由「能不能在它旁边建个目录」实测，不按路径猜；`/Applications` 默认就写得了，通常一个密码框都不弹；搬不进去就把旧的放回原位），Windows 走 NSIS `/P` 被动模式。**F2 删除计划的体积虚高约 15 倍且两行一模一样** —— 改按**实占块数**（`st_blocks × 512`）统计（虚拟机磁盘是稀疏文件），「工作目录」把 `runtime` 刨掉，「不能同时删运行环境」那段警告不再连说两遍。**F3 重装不沿用保留下来的 key** —— 要 key 之前先读 `~/.hunter/app/.env`，格式对就当场验一次，验得过直接用并说一句「沿用上次保留的 key」；命令行与界面（新命令 `kept_key`，**完整 key 不出 Rust 进程**）都是这样。**F4 重装后定时备份任务「回来没有」看不出来** —— 那件事本来就会做，但只写在日志里而日志刚被删掉；现在它是安装最后一步的一条结论，三种结局各说各的话，删除时也提前说明「重装后会自动装回」。守卫**新开一道窄门**（`argv_privileged` 那张表上的第三条形状：macOS 换包那条 `rsync -a --delete`，两头都钉死），通用那道门一个字没松 | `plan/mac-0.1.13-20260923/本机验收结果.md`（本地 Claude 2026-09-23 23:32–23:39 真机逐条实跑） | **完成**（macOS 的自更新与重装待用户本机验收） | 2026-09-24 |
| **I9** | **0.1.8 真机首测的三个 P0**：上一次装剩的残骸被当成「用户装的 Docker / Colima」（内置主线一步没走到）、裸 `colima start` 在用户家目录里建出一台没人要的虚拟机、修好之后不接着装 + 端口探测打错 socket | 用户 Mac 真机跑 0.1.8（`plan/mac-0.1.8-20260922/`） | **完成**（macOS 真机待用户本机验收） | 2026-09-22 |

## 三、里程碑成果文档

| 文档 | 说明 |
|---|---|
| [M0-预研结论.md](M0-预研结论.md) | 七项实测的命令、原始输出、结论，以及对 M1–M4 的影响 |
| [M1-成果与测试报告.md](M1-成果与测试报告.md) | 界面骨架、设计令牌、状态机、CI，以及与三张视觉稿的逐张比对 |
| [M2-成果与测试报告.md](M2-成果与测试报告.md) | Rust 五个模块 + 编排层 + headless；十项真实测试；真实数据截图；本轮撞出来的 5 个 bug |
| [M3-成果与测试报告.md](M3-成果与测试报告.md) | 运行面板 / 托盘 / 设置 / 日志 / 反馈 / 遥测 / Linux 安装包；国内源香港区从零拉验证；15 项真实测试；18 张截图；本轮撞出来的 9 个问题 |
| [M4-成果与测试报告.md](M4-成果与测试报告.md) | 发布流水线 / 自更新 / 升级回滚 / 离线包；真机测试用例与实测数字；未签名包在三平台的放行说明 |
| [I14-迭代报告.md](I14-迭代报告.md) | **用户 Mac 上撞出来的四条**：macOS 自更新按平台取包并就地换 `.app`（0.1.13 在 mac 上下的是 `.deb`）、删除计划的体积改按实占块数算且两行分开、重装沿用留下来的 key、重装后定时备份任务的结论说出来。四条里有三条的共同点值得记一笔：**代码里那件事其实做了，缺的是「说出来」** —— F4 尤其典型，`flow::start` 装完本来就会挂定时任务，但它只往日志里写了一行，而 `--uninstall` 刚刚把日志目录删掉了 |
| [I13-迭代报告.md](I13-迭代报告.md) | **数据有了退路**：R6 备份与恢复（`pg_dump -Fc` + 密钥卷 + 校验 + 按天轮换 + 三平台定时任务 + 六步恢复 + 任意目录迁移）、R4 删除应用（两档 + 逐字输入 + 只动本项目的五道判据）、R7 七类异常监测（结论 + 一键处理 + 脱敏快照交给诊断助手）。三条设计上的硬规矩：**没校验过的不算备份**（`pg_restore --list` 读不出目录就判失败 —— 一份「看起来在那儿、其实恢复不了」的备份比没有备份更糟）、**行数是 `count(*)` 数出来的不是 `n_live_tup` 估的**、**一条 `prune` 都不跑**（测试机上同时跑着另外三套，一条 `prune -a` 能把它们一起清掉）。守卫三道新窄门，通用那道门一个字没松 |
| [I12-迭代报告.md](I12-迭代报告.md) | **装好了就记住**：判据从「只看标记」改成「现状优先」、三层资源监控、停启重启补全、重装前只读检测已有数据。实测撞出两条「验收工具自己在说假话」（`--monitor` 的进位基数和界面不是一套、发布后自查读了一个会过期的端点） |
| [I11-迭代报告.md](I11-迭代报告.md) | **失败之后的那 71 分钟**：0.1.9 在用户 Mac 上 21:39 报「启动超时」进错误页，22:05 根因在外部被修好（6/6 健康、网页 200），界面一直到 22:50 还挂着 21:39 那张卡片；用户唯一能点的「重试」点下去是重新取 compose、重写 `.env`、**重新下载 849 MB**。本轮五件事：**新模块 `selfcheck.rs`**（只读复查，四种结论 Absent / Incomplete / Partial / Healthy，其中「建出来过但一次都没跑起来」的容器一律归 Incomplete —— 它们身上冻着上一次那组端口）+ 错误页低频复查（挂载时 / 每 20 秒 / 窗口回焦 / **AI 与规则层每跑完一个动作**）+ 新事件 `ALREADY_RUNNING` 直落运行面板（不经过完成页，那一页会说「装好了」而这次一字节没下）；**「重试」先验证不重装**（Healthy → 零动作；Partial → 只 `restart` 不健康的、只 `up -d --no-recreate` 停着的，健康容器一个不碰；`Absent`/`Incomplete` 才完整安装），外加一条单测钉死「启动器自己装的那一套永远不算『另一套 Hunter』，绝不并出第二套」；**「关闭」**（`window_hide`，回托盘不退出不停容器，复查无异常时升为主按钮）；**端口三分类**（`Verdict` 拆出 `free` / `usable()` / `Kind::{Free,Mine,Other}`，根因是旧代码发现「占着它的是我们自己」时直接 `return Verdict::free(port)` —— 对安装是对的，对诊断是错的；顺带让 `--diagnose` 与界面报文共用同一个 `ports_section()`）；**手工 compose 也带覆盖文件**（`.env` 写 `COMPOSE_PROJECT_NAME` / `COMPOSE_PATH_SEPARATOR` / `COMPOSE_FILE` 并进「写完读回来对账」名单 + 「跑着的容器绑在哪 vs 配置说该绑哪」的开机自查与自动收回，背景是 2026-09-22 22:01 手工漏覆盖文件导致五个端口在局域网可达约 50 分钟）。实测：已健康时点重试 **4 秒 · 0 命令 · 0 token · 零重建**（首装 72 秒 → 重试 2 秒），只停一个服务时只跑一条 `up -d --no-recreate opencode`，真界面上外部修好到界面自己切回运行面板 **≤ 30 秒零点击**；I5–I10 回归六项全绿（守卫 8 条全拒、旧栈容器 ID 与卷一个没变）。**实测撞出三条**（收尾那句话在说假话、`--diagnose` 根本没有端口这一节、`Created` 容器差点被当成「只差起一下」）。Rust 单测 547 → **563**、前端 85 → **90**；截图 24 + 3 张；`launcher-v0.1.11` |
| [I10-迭代报告.md](I10-迭代报告.md) | **内置虚拟机没有 DNS**：0.1.9 在用户 Mac 上该做的都做对了（认残骸、走代理取 compose、腾讯云香港 693 秒拉完 849 MB、`compose up` 成功、全程零点击），卡在最后一米 —— opencode 13 分钟不健康、`E_START_TIMEOUT`、规则层 unknown。**根因用日志永远看不出来**：把启动器真正下发的那个 disk-image（sha256 与清单一字不差）解开挂载逐条查实 —— 镜像里**没有 systemd-resolved**（Ubuntu 24.04 把它拆成了单独的包，minimal 不装），而 `/etc/resolv.conf` 出厂就是指向它的断链；lima 写 DNS 的两条路（cloud-init 的 `resolv_conf` 模块 / netplan 的 `nameservers`）在这份镜像上**都是死路**，所以 `colima start --dns` 治不了本。修法：新模块 `runtime/vmdns.rs` 自己在**那台虚拟机**里把 resolv.conf 写成普通文件（网关 `192.168.5.2` + 国内可用的 `223.6.6.6` / `119.29.29.29`，数值全部实测）+ 装一个开机重写它的 oneshot systemd 服务 + 每次启动复查；**「容器能不能连上模型网关」升成必检项**（起一个 `--rm --pull=never` 的一次性容器真 curl 一次，不带 key）+ 两个新错误码 `E_RUNTIME_NO_DNS` / `E_CONTAINER_OFFLINE` + 规则层确定性识别；**守卫另开一道窄门**（10 条逐字写死的命令，总门对 `/etc/resolv.conf` 的禁令一个字没松）；**把用户 Mac 那一幕在 Linux 上原样复现**（`dockerd --dns <不可达>` → 5/6 健康、opencode unhealthy）。**实测当场抓出三条**：新代码差点把「本机没有这个镜像」报成「容器连不上网」、「生成诊断包」被当成一次修复空转 4 回合 22,400 token、错误页上原样印着 Markdown 星号。Rust 单测 527 → **547** 条、前端 85 条；`launcher-v0.1.10` |
| [I9-迭代报告.md](I9-迭代报告.md) | **「上一次没装成功」这件事**：0.1.8 在用户 Mac 上把**自己上一次留下的残骸**当成了「用户装的 Docker / Colima」，内置主线一步没走到。四个 P0 —— **新模块 `runtime/effective.rs`**（「现在生效的运行时是谁」只判一次：内置在跑 → 你的 Docker 在跑 → 内置装了没起 → 你的装了没起）+ 新错误码 `E_BUILTIN_DOWN` + 动作 `repair_builtin_runtime`（清 profile 重建，**只动 `~/.hunter/runtime`**）；**colima 三道隔离**（子进程环境钉死 `COLIMA_HOME` / `LIMA_HOME`、动作表分清「我们的」和「他的」、**守卫挂在真正 `spawn` 之前**，所以不管命令是谁写的都拦得住）；**修好就接着装**（动作失败可换办法、全自动档下 Safe 动作自己跑掉、Docker 一可用且没装完就自动续装，带断路器）；**端口探测走对 socket**（`DOCKER_HOST` 由「当前生效运行时」给，不再打到 `/var/run/docker.sock`）。测试机 7 场景实测：残骸 + 用户 Docker 在跑 **零点击 106 秒到 6/6 · 0 token**，假 colima 收到的命令**全部**带 `--profile hunter`，跑完 `~/.colima` 仍不存在；**实测撞出 7 个问题**，最难看的是我们的残骸会把用户一套好好的 Docker 弄坏（残骸里坏掉的 compose 插件被 `DOCKER_CONFIG` 用上，读代码时那行看起来完全合理，只有真跑才炸得出来）；**刻意没做**的一件事：不去碰 `~/.colima/default`（判据判不准，判错就是停掉用户自己在用的虚拟机，见报告第八节）。Rust 单测 482 → **518** 条、前端 85 条；发布后自查固化成 `scripts/release-selfcheck.sh`；`launcher-v0.1.9` |
| [I8-迭代报告.md](I8-迭代报告.md) | **「绝不让用户自己动手」落地**：界面上不再有任何「请在终端里执行 …」「复制这条命令」「我执行完了，继续」，并加了一条流水线检查（`scripts/check-wording.py`，七条正则 + 9 条反例 / 8 条正当文案的自测）把它钉死；**全自动**（默认档位 `confirm` → `auto`，授权页只剩一个「开始安装」，档位选择挪到设置页；本机已有另一套 Hunter 自动选「并存、换端口」；`Sensitive` 由守卫 + 复核员两道门把关后直接执行）；**需要管理员权限时弹系统自己的密码框**（macOS `osascript` / Linux polkit / Windows UAC，不自绘、不存密码、能提权的事写死在一张两条的表里，提权前再过一遍守卫）；**0.1.7 真机首测的两个根因**：虚拟机系统镜像（colima-core v0.10.4，341/316 MB）纳入启动器自己的下载流水线（双源 + sha512 + 真实进度 + `colima start --disk-image`）、**只读地沿用用户自己配好的系统代理**（`scutil --proxy` / 注册表 / 环境变量，传给 colima / limactl / docker 与虚拟机，绝不修改）；**没有 Docker 时三条路自动依次走完**（内置运行时 → OrbStack dmg → Homebrew，没有 brew 就先替用户装 brew，排序原则是「同样能解决问题时优先不需要交互的那条」）；协议 MIT → **Apache-2.0**（新增 NOTICE 与 THIRD_PARTY_NOTICES）；**实测撞出 5 个问题**，其中最要紧的一个是本轮自己引入的 —— 取消「Sensitive 要用户点一下」之后，「方向该由用户定」的那一类动作裸奔了（动作表新增 `user_only`，档位管不着它），抓到它的是 I5–I7 的回归而不是自审；Rust 单测 446 → **482** 条、前端 81 条；`launcher-v0.1.8` |
| [I7-迭代报告.md](I7-迭代报告.md) | 复核员（第二个模型，独立提示词、不给工具表；解析不出结论一律判「没通过」，**不默认放行**）；内置运行时（Colima + Lima + docker CLI + compose，版本与 sha256 **写死在清单里**、下完逐个校验、两个源并列测速，全部落在 `~/.hunter/runtime`、不要管理员密码、可一键卸载）；OrbStack dmg 备选路线（验签三道含 Team ID）；接管本机已有的那一套（卡片**不阻塞**、改动类一律二次确认、任何情况下不删它的卷）；一键反馈直达（出口闸新增用户名与主机名扫描）；预检换端口算「已解决 1 个」、事件流按次留 5 份、规则层先硬找 docker、命令行 confirm 档按 `IsTerminal` 真的问；**免费版只允许本机访问**（用户 2026-09-21 19:05 拍板：覆盖文件里六个服务全部 `127.0.0.1`，绑定地址由 `WebBind::detect()` 从磁盘现状读、不由配置决定，手改 `launcher.toml` 放不开且会留一条日志；老机器升级不自动改动，运行面板给一行提示 + 单向的「一键收紧」）；**实测撞出 9 个问题**，其中一个是 `proc::run_timeout` 从 M2 藏到现在的 64 KB 管道死锁，还有一个是界面文案里的 `**粗体**` 一直被原样印出来（截图抓的，已加一条 i18n 单测钉死）；Rust 单测 446 条、前端 81 条、截图 24 张；`launcher-v0.1.7` |
| [I6-迭代报告.md](I6-迭代报告.md) | 子进程环境统一构造（PATH 补全 + `DOCKER_CONFIG`，本轮 P0）；compose v5 顶层错误的「原话」不再丢；新错误码 `E_CRED_HELPER`；不带 credsStore 的隔离 docker 配置（**不改用户文件**，守卫钉死）；同一条原话只换一次源；**在 Linux 上用真的 0.1.5 二进制复现了失败**，新版零点击到 6/6；383 个 Rust 单测；`launcher-v0.1.6` |
| [I5-迭代报告.md](I5-迭代报告.md) | 端口探测三重确认（本轮 P0）；预检闸门；`E_PORT_CONFLICT`；一次授权 + 多智能体自动安装；守卫与审计；七场景实测（**授权后零点击到 6/6，54–94 秒**）；GUI 真机全流程；17 + 11 张截图；本轮自己撞出来并修掉 8 个 bug；`launcher-v0.1.5` |
| [I1-迭代报告.md](I1-迭代报告.md) | P0 清零（项目名互顶修掉、402 实测、三条转上游并提 issue）；自审与回归各抓出 3 个新问题；8 个用例的实测；16 张截图；`launcher-v0.1.1` |
| [I2-迭代报告.md](I2-迭代报告.md) | 回归撞出来的 P0（随机口令里的 `/` 让整套约 40% 概率装不起来）；自审四条（URL userinfo 绕法、`.env` 换行注入、「已复制」撒谎、子目录 775）；待办池四条（429 实测、deb 版本号、COS 清理、复制按钮）；P1-20 的开关做完但默认值仍等用户决定；6 个用例、20 张截图；`launcher-v0.1.2` |
| [I3-迭代报告.md](I3-迭代报告.md) | 回归撞出来的「额度真用完了，运行面板一个字都没提」；Podman 真装一次验到底（发现代码里的假设是反的，第一版修法又栽在 `argv[0]` 上）；自审六条（更新页标题自相矛盾、UTC 时间戳上屏、裸数字、`--tray-menu` 打错字返回 0、`--key-file` 不查权限、一条跟着机器状态走的单测）；待办池三条（P1-22 / P2-16 / P2-17）；`.env` 写完读回来对账；GUI 回归的点击断言**第一次用就抓到一次点空**；6 个用例、18 张截图；`launcher-v0.1.3` |
| [I4-迭代报告.md](I4-迭代报告.md) | **用户 Mac 真机首测撞出的 P0**：macOS GUI 程序拿不到 shell 的 PATH，装着 OrbStack 也会报「没有找到 Docker」→ 改成「配置 → PATH → 平台已知位置」按顺序探并把实际路径上屏；向导顺序改成「欢迎 → 输入 key → Docker → …」；**两层的 AI 诊断助手**（规则层零 token / AI 层最多 3 轮 + 12 条动作白名单 + 8 条降级）；本轮实测又抓出 5 个真 bug（回灌带厂商私有字段导致第 2 轮必然 400、安装失败一律报成拉取失败、规则被平台差异静默吞掉、自己占的端口算成冲突、项目名冲突没规则）；四个场景 + 降级矩阵 + 安全用例全部真跑；24 张截图；`launcher-v0.1.4` |
| [待办池.md](待办池.md) | 发现了但当期不做的问题，按 P0/P1/P2 排；迭代轮从这里取活 |

M1 之后每个里程碑追加一份 `<里程碑>-成果与测试报告.md`，截图放 `docs/screenshots/<里程碑>/`。

## 四、当前已知的关键约束（M0 实测得出，详见预研结论）

| 约束 | 影响的里程碑 |
|---|---|
| 网关四类 key 错误（格式错 / 不存在 / 已吊销 / 未带）返回**完全相同**的 401 体，无法区分 | M2 已按此实现（本地判格式 + 401 统一文案） |
| **`docker compose --progress json pull` 的进度流在 stderr 上**，不是 stdout（M2 实测修正 M0 §5.1） | M2 已修 |
| `Extracting` 行**只有** `current` 没有 `total`，算不出解压百分比 | M2 已按此实现（显示「解压中」） |
| `docker pull` 在非 TTY 下**完全没有**逐层进度，只有三行结果 | M2 已绕开（走 compose） |
| hunter-community 的 Release **没有任何 assets** | M2 已绕开（raw + sha256 校验 + 内置副本兜底） |
| 阿里云 ACR 与 Docker Hub 上都没有镜像；国内源改用**腾讯云香港** `hkccr.ccs.tencentyun.com/agentpit`（广州区已停用） | M2 已接入（两源真实测速） |
| GHCR 镜像六个合计 **849 MB**（amd64 压缩，manifest 实测），不是视觉稿写的 2.1 GB | M2 已按实测上屏 |
| **两个工作目录会抢同一个 compose 项目名 `hunter`**，后开的会顶掉先装好的（M2 实测撞到） | **I1 已修**：动容器前比 compose 标签，不是自己的就报 `E_PROJECT_CONFLICT`。**I4 补**：错误码真的传到界面（原来被一律当成 `E_PULL_FAILED`），并给了一条专门的诊断规则 |
| **macOS 上从访达/程序坞启动的 GUI 程序，PATH 只有 `/usr/bin:/bin:/usr/sbin:/sbin`**（`launchctl getenv PATH` 为空 → 走系统默认），不含 `/usr/local/bin` —— 而 OrbStack / Docker Desktop / Intel 版 Homebrew 的 docker 都在那里（用户 Mac 真机实测） | **I4 已修**：`runtime::which` 按「配置 → PATH → 平台已知位置」探，界面上显示实际用的是哪一条；清单可从 `[runtime]` 覆盖 |
| **这个网关（后接 Gemini）在 `tool_calls[].extra_content.google.thought_signature` 里塞自己的内部状态，原样回灌会让上游回 HTTP 400**（I4 三变体对照实测） | **I4 已修**：回灌前只留 OpenAI 协议本身的字段 |
| 方案第 13 节列的**六个 api 新增接口全部不存在**；M3 把 api 容器上能读的接口逐条探了一遍，不登录能读的只有 `/api/health`、`/api/setup/status`、`/api/setup/llm/quota`、`/api/setup/env-check` 四条 | M3 已按此实现：「数据源」卡接上 `/api/setup/status`，「今日对话」「晨报」显示「—」并在界面上列出缺哪四条接口（M3 §14） |
| `.deb` **3.29 MiB** 远低于目标，但 **`.AppImage` 75.09 MiB 超出「≤40 MB」近一倍**（自带 WebKitGTK，压不下去。M3 真打真装复核过） | M4 · 发布策略与 README 文案 |
| 安装包文件名带空格（Tauri 用 `productName`），下载地址会变成 `%20`；CI 里已改名成 `hunter-launcher_<版本>_amd64.*` | **M4 已处理**：release.yml 用同一套命名，`selfupdate.rs` 有测试盯住两边一致 |
| **Tauri 的 updater 在 Linux 上只能就地更新 AppImage**，`.deb` 装在 `/usr` 下要 root | M4 已按此实现：`.deb` 走「下到 `~/.hunter/updates/` + 给一条 `sudo apt install` 命令」 |
| **腾讯云 COS 拒绝 path-style 寻址**（`PathStyleDomainForbidden`），必须 virtual-hosted | M4 · `scripts/cos-upload.py` 已按此写 |
| `createUpdaterArtifacts` 打开后 `tauri build` **必须**有 `TAURI_SIGNING_PRIVATE_KEY`，否则打完包最后一步报错 | M4 · ci.yml 用 `--config` 关掉它，签名只在 release.yml 里做 |
| **没配的 secret 在 job 级 `env` 里是「存在但为空」**，Tauri 判的是「变量在不在」→ 会拿空身份去 codesign 然后失败 | M4 · `tauri build` 之前把空的显式 `unset` |
| **MSI（WiX）只认纯数字的预发布段**，`0.1.0-rc.1` 直接被拒 | M4 · Windows 分两次打包，MSI 用 `wix.version` 覆盖成数字版本 |
| **macOS 的 `.app.tar.gz` 是唯一文件名里不带版本的产物**，不改名的话 `latest.json` 里会缺 darwin 两项**而流水线是绿的** | M4 · 改名时补版本 + 加「四个 target 键一个都不能少」的闸门 |
| **COS 不支持 boto3 的分片上传**（UploadPart 要 Content-Length），小文件成功、大文件必失败 | M4 · `TransferConfig` 关掉 multipart |
| **COS 的批量 `DeleteObjects` 要带 `Content-MD5` 头**，boto3 不带 → 整批失败（I2 第一次真跑清理时撞到）。和上一条同源：S3 兼容不等于全兼容 | I2 · `cos-prune.py` 改成逐个 `delete_object` |
| **GitHub 的 `releases/latest/download/…` 解析的是「最新的正式 Release」**，全是 prerelease 时它 404 | M4 · updater 的 GitHub 端点当前是死的，转正式版那天会自己活过来 |
| 字体是**本地打包的子集**，只含仓库里出现过的字形；容器日志里的中文（如「迁移」）靠系统中文字体回落（M3 装 `fonts-noto-cjk` 前后对比验证过回落有效） | 所有涉及界面的里程碑 · 加新文案要跑 `scripts/build-fonts.py`，CI 的 `check-fonts.py` 会拦 |
| AppImage 打包需要 `xdg-utils`（Tauri 官方依赖清单里没有），缺了会在最后一步失败 | M4 · CI 依赖 |
| 2 核 8G 机器上 Tauri 冷编译耗时与峰值内存见预研结论第六节 | M1 · CI 超时设置 |
| `cargo build --release` **不等于** `tauri build`：前者不带 `custom-protocol` 特性，产物会去连 devUrl 而不是内嵌前端，表现为白屏（M1 §9 实测） | M4 · 打包脚本 |
| 视觉稿是 2 倍图（2360×1520 ↔ 约 1180×760 逻辑像素），量到的像素值要 ÷2 | 所有涉及界面的里程碑 |
| **数据卷跟着 compose 项目名走，不跟着工作目录走**（`hunter_hunter_pg_data` 等）。换工作目录时新建空目录会现生成一把新的 `POSTGRES_PASSWORD`，api 报 `password authentication failed`（I1 实测） | I1 已按此写提示：要换位置就整个目录搬过来 |
| **额度用尽时 `/quota` 回的是 HTTP 200**（体里 `exhausted: true`），402 只出现在 `POST /v1/chat/completions` 上 —— 靠 402 判「额度用尽」是判不出来的（I1 实测） | I1 已改成读 `exhausted` 字段 |
| **没有 `DISPLAY` 时 tao 会在 GTK 初始化处 panic 并 core dump**（退出码 134），而 `--tray-menu` 本来就是写给 SSH 用户的 | I1 已加前置判断，改成退出码 2 + 中文提示 |
| **web 端口按设计绑 `0.0.0.0`**：I1 实测从另一台机器不需要任何凭证就能进聊天界面并消耗用户额度；上游 api 只保护了 `/api/setup/*`（`no_token_public` 401），没保护聊天本身 | 待办池 P1-20 · **需要用户决定默认值** |
| 测试机 pnpm 12 的 `minimumReleaseAge` 现在会挡住 `pnpm exec` / `pnpm run`（不只是 `install`），`tauri build` 因此走不下去 | I1 绕法：直接用 `node node_modules/…/vite.js` 与 `tauri.js` + `--config` 清空 `beforeBuildCommand` |
| **启动器生成的口令绝不能含 `/` 或 `+`**：上游把 `POSTGRES_PASSWORD` / `OPENCODE_PASS` 拼进 URL，而 URL 的 authority 到第一个 `/` 就收尾（RFC 3986）。标准 base64 生成的 32 字符里出现 `/` 的概率 **39.6%**，算上 `+` 是 63.5% —— I2 的 GUI 回归真撞到一次，api 容器起不来、`up -d` 直接失败 | **I2 已修**：`secretgen::random_token()` 只用 `[A-Za-z0-9]`。已写进 `.env` 的旧口令不换（换了连不上已初始化的数据卷），但会被认出来并写一条 warn |
| **网关限流（429）只作用于 `POST /v1/chat/completions`**，窗口是**自然分钟**不是滑动窗口；`/v1/models` 连打 30 次、`/quota` 在触发后立刻查都是 200（I2 实测） | 启动器只用 `/quota` 与 `/v1/models`，碰不到限流；仍补了 `E_RATE_LIMITED` 免得哪天扩大范围后显示成「key 无效」 |
| **浮层（日志 / 反馈 / 设置 / 更新）的按钮在 `y = 91`，不是标题栏那一行的 `y = 44`**；GUI 回归脚本用错坐标时浮层关不掉，后面几张会全是同一页 | I2 起 `scripts/check-shots.sh` 会抓「两张一模一样」——本轮当场抓到一次 |
