# HunterLauncher · 总进度表

> 时间一律**上海时间**。状态只有四种：`未开始` / `进行中` / `完成` / `失败`。
> 「完成」= 代码合进 main + 在测试机上真实跑通 + 有截图 + 成果文档已写。
> 单项做完但没通过测试的，状态仍是「进行中」。

最后更新：2026-09-20（M2 收尾）

---

## 一、里程碑

| 里程碑 | 目标 | 交付物 | 状态 | 完成时间 |
|---|---|---|---|---|
| **M0** | 预研与仓库骨架 | 网关/镜像/Docker/Tauri 七项实测；README、`.gitignore`、`docs/`、总进度表、待办池 | **完成** | 2026-09-19 |
| **M1** | Tauri 骨架 + 设计令牌 + 向导壳 + CI | 三平台可编译；深海军蓝 + 琥珀金设计令牌；13 个页面（向导 7 + 运行面板 + 设置 / 日志 / 反馈 / 错误页）；状态机纯函数 + 47 个单测；`ipc.ts` 与演示数据模式；本地打包字体；GitHub Actions CI | **完成** | 2026-09-19 |
| **M2** | Rust 核心 + 真实向导全流程 + headless | `runtime::docker`、`config`、`gateway`、`registry`、`compose`（`--progress json` 流式解析）、`flow` 编排、`--headless`；前端接真实 IPC 与事件流；106 个 Rust 单测 | **完成** | 2026-09-20 |
| **M3** | 托盘 + 升级 + 备份 + 运行面板打磨 | 托盘菜单与开机自启、Hunter 版本检查与升级（升级前 `pg_dump` 备份、失败回滚）、运行面板细节、离线包导入 | 未开始 | — |
| **M4** | 打包 + CI + 自更新 + 反馈 | 三平台 CI 矩阵、`.deb`/`.AppImage` 真装真跑、updater 签名链路、诊断包导出与提 issue、`launcher-v0.1.0` | 未开始 | — |

## 二、迭代轮

| 轮次 | 主题 | 取活来源 | 状态 | 完成时间 |
|---|---|---|---|---|
| **I1** | 从待办池取 P0 | [待办池.md](待办池.md) | 未开始 | — |
| **I2** | 从待办池取 P0/P1 | [待办池.md](待办池.md) | 未开始 | — |
| **I3** | 从待办池取 P1/P2 + 收尾 | [待办池.md](待办池.md) | 未开始 | — |

## 三、里程碑成果文档

| 文档 | 说明 |
|---|---|
| [M0-预研结论.md](M0-预研结论.md) | 七项实测的命令、原始输出、结论，以及对 M1–M4 的影响 |
| [M1-成果与测试报告.md](M1-成果与测试报告.md) | 界面骨架、设计令牌、状态机、CI，以及与三张视觉稿的逐张比对 |
| [M2-成果与测试报告.md](M2-成果与测试报告.md) | Rust 五个模块 + 编排层 + headless；十项真实测试；真实数据截图；本轮撞出来的 5 个 bug |
| [待办池.md](待办池.md) | 发现了但当期不做的问题，按 P0/P1/P2 排；迭代轮从这里取活 |

M1 之后每个里程碑追加一份 `<里程碑>-成果与测试报告.md`，截图放 `docs/screenshots/<里程碑>/`。

## 四、当前已知的关键约束（M0 实测得出，详见预研结论）

| 约束 | 影响的里程碑 |
|---|---|
| 网关四类 key 错误（格式错 / 不存在 / 已吊销 / 未带）返回**完全相同**的 401 体，无法区分 | M2 已按此实现（本地判格式 + 401 统一文案） |
| **`docker compose --progress json pull` 的进度流在 stderr 上**，不是 stdout（M2 实测修正 M0 §5.1） | M2 已修 |
| `Extracting` 行**只有** `current` 没有 `total`，算不出解压百分比 | M2 已按此实现（显示「解压中」） |
| `docker pull` 在非 TTY 下**完全没有**逐层进度，只有三行结果 | M2 已绕开（走 compose） |
| hunter-community 的 Release **没有任何 assets** | M2 已绕开（raw + sha256 校验 + 内置副本兜底） |
| 阿里云 ACR 与 Docker Hub 上都没有镜像；国内源改用**腾讯云香港** `hkccr.ccs.tencentyun.com/agentpit`（广州区已停用） | M2 已接入（两源真实测速） |
| GHCR 镜像六个合计 **849 MB**（amd64 压缩，manifest 实测），不是视觉稿写的 2.1 GB | M2 已按实测上屏 |
| **两个工作目录会抢同一个 compose 项目名 `hunter`**，后开的会顶掉先装好的（M2 实测撞到） | M3 · 待办池 P0-5 |
| 方案第 13 节列的**六个 api 新增接口全部不存在**（源码里一行都没有） | M3 · 运行面板两张卡片无数据来源 |
| `.deb` 2.28 MiB 远低于目标，但 **`.AppImage` 74 MiB 超出「≤40 MB」近一倍**（自带 WebKitGTK，压不下去） | M4 · 发布策略与 README 文案 |
| AppImage 打包需要 `xdg-utils`（Tauri 官方依赖清单里没有），缺了会在最后一步失败 | M4 · CI 依赖 |
| 2 核 8G 机器上 Tauri 冷编译耗时与峰值内存见预研结论第六节 | M1 · CI 超时设置 |
| `cargo build --release` **不等于** `tauri build`：前者不带 `custom-protocol` 特性，产物会去连 devUrl 而不是内嵌前端，表现为白屏（M1 §9 实测） | M4 · 打包脚本 |
| 视觉稿是 2 倍图（2360×1520 ↔ 约 1180×760 逻辑像素），量到的像素值要 ÷2 | 所有涉及界面的里程碑 |
