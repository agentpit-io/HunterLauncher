# 更新日志

本文件记录**用户看得见的**变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。时间一律上海时间。

`scripts/release-notes.sh` 会把对应版本的小节原样抄进 GitHub Release 说明，
所以这里只写变化，下载地址、未签名放行步骤那些每版都一样的内容由脚本补。

---

## [0.1.16] - 2026-09-27

迭代轮 I16。一句话：**拉镜像卡住了启动器自己会发现，出了事你点一下就能把日志交给我们。**

这一版另外修了四处**只在 Windows 上暴露**的问题 —— 它们来自一位 Windows 用户 2026-09-26
发来的诊断包。其中「定时备份从装机第一天起就没装上」这一条最要紧。

### 新增

**拉镜像卡住了会被认出来，不再干等（重要）。** 有一类连接不会报错：TCP 连上了、
docker 的进程还活着、就是一个字节都不传。0.1.15 及之前的启动器会陪着它一直等下去 ——
2026-09-25 有用户升级时真撞上了，界面上「正在拉取新版本的镜像…」停了一个多小时，
最后只能强退。现在**90 秒没有任何新数据进来就判定这一路卡死**，主动把它掐掉、
换另一个镜像源重来；两个源都这样才算失败。新错误码 `E_PULL_STALLED`，
界面上说的是「从「腾讯云 · 香港」拉了 1 分 30 秒，已下载 12.4 MB / 共 748 MB」，
而不是一句笼统的「拉取失败」。网络特别慢的机器可以在 `~/.hunter/launcher.toml` 里
用 `[hunter] pull_stall_secs` 把这个阈值调长。

**一键上传日志（重要）。** 遇到问题时，原来要你自己导一个诊断包、再把文件发给我们。
现在错误页、设置页、运行面板上都有一个「上传日志给我们（不含 key 与口令）」：
点一下 → **先把要传的内容原样摆给你看** → 你确认 → 脱敏后的日志进我们的排障库，
屏幕上给你一个 8 位追踪码（形如 `HL-7K3Q9F`），把这个码发给我们就能查到那份日志。

* **默认不传。** 只有你点了才发。设置页有一个「出错时自动上传日志」的开关，
  **默认关**，打开之后也只在出错时传，正常运行一个字节都不出门。
* **key 一个字节都不上传**，口令、邮箱、手机号、IP 末段、你的用户名与主机名、
  你和模型说过的话，全部抹掉；出门前还会整份再扫一遍，扫出东西就不给传。
* 机器标识是一串随机 UUID（不含网卡地址、序列号、主机名），只用来把同一台机器的
  几次上报串起来，删掉 `launcher.toml` 就会换一个。
* 传不上去时会说人话，并**当场给你导一个诊断包**，发那个文件一样管用。

**升级过程中可以取消。** 点「取消升级」就停在当前这一步，并把配置写回升级前那一份 ——
界面会明说「Hunter 还是 v1.2.0，正在跑的 6 个容器一个都没动」。

**「上一次升级没做完」会被认出来。** 强退发生在「配置已经改成新版本、镜像还没拉全」
之后时，机器会停在一个说不清的中间态，下次打开照着新配置起容器只会**再卡一次**。
现在开机自查会发现它，运行面板顶上给两个按钮：「继续升级」或者
「回退到正在跑的那一版」（只改配置，不动容器、不动数据、不拉镜像）。

### 修复

**日志里的口令又多挡住了两种形状。** `XXX_PASSWORD=` / `SECRET=` / `PASS=` /
`TOKEN=` 这一类键值对，以及连接串里的 `postgres://用户:口令@主机`，
现在在所有出口（日志、诊断包、上传的正文）都会被打码。
原来这两种只在特定路径上被挡住，用户手改过的 `.env`、容器日志里打出来的连接串不在其中。

**弹窗内容多的时候不再把标题和按钮顶出屏幕。** 弹窗现在有高度上限，
中间那段自己滚动，标题与按钮永远看得见。

**（Windows）每日自动备份没挂上时，界面上会说出来（重要）。**
0.1.15 装机时挂不上定时任务，只往日志里写一行「不影响安装」，界面上一个字都没有 ——
于是你以为自己有每日自动备份，实际上**从第一天起一次都没跑过**。
现在运行面板上会出一条红色横幅说明这件事，带一个「再挂一次」按钮；
挂任务本身也从一条路变成三条（`/XML` → 先删旧任务再试 → 完全不用 XML 的
`/SC DAILY`），走到第三条时会明说「这条路装出来的任务错过了不补跑」。
另外修掉一个真 bug：临时任务文件用的是固定文件名，安装收尾与开机自查同时跑时，
一边会把另一边正在读的那份删掉。

**（Windows）日志里的中文不再是 `����` 这样的乱码（重要）。**
中文 Windows 上很多系统命令按 GBK 输出，0.1.15 一律按 UTF-8 解。
后果不只是难看 —— 真正的失败原因**我们一次都看不到**。
现在先按 UTF-8 解，解不动才按这台机器的控制台代码页重解一遍。
顺带修掉一处更隐蔽的：以前只要子进程吐出一行不是 UTF-8 的内容，
**后面所有输出（包括拉取进度）就全丢了**，而且没有任何迹象。

**容器「退出了」不再被说成「还没就绪」。** 六个容器全停着的时候，
0.1.15 说的是「Hunter 在跑，但 web、api… 还没就绪、网页打不开」，
顶上那行大字还是「Hunter 运行中」——和下一行的「容器已停止」自相矛盾。
这两件事该做的动作完全相反（一个点「启动」，一个等一等或看日志）。
现在说的是「Hunter 装好了，但 6 个容器都停着 —— 点「启动」就能用」，
顶上那行大字也跟着「现在到底有没有容器在跑」走。

**选镜像源时会真下一段层数据，不只看镜像列表通不通。**
有的网络上镜像列表又快又通、真正的镜像数据却一个字节都下不来
（GHCR 的层数据在另一个主机上）。0.1.15 每次都会选中这种源，然后卡住。
现在测速时会挑最小的一层真下 256 KB 回来，下不来就判这个源不可用。

**镜像本机已有时不再说「拉取完成，用时 4 秒」。** 那句话让人以为下载成功了，
于是后面真要下载时卡住就显得莫名其妙。现在说的是「6 个镜像本机都已有，没有下载」，
真下载了才报用时和字节数。

## [0.1.15] - 2026-09-25

一句话：**新装直接装最新版 Hunter，不再装启动器发版那天的旧版。**

### 修复

**新装的 Hunter 版本总是落后。** 以前新装一律用启动器编译时内置的版本号 —— GitHub 上
Hunter 早已发到 1.2.1，0.1.14 新装出来的仍是 1.2.0，缺了「对话框的模型选择器显示真实
模型名」等修复，要装完再点一次「升级」。现在安装前先查一次 Hunter 最新正式版，**比内置的
新就装新的**，安装日志里会写一句「安装最新正式版 Hunter 1.2.x」；查不到（离线、GitHub
不通）照旧按内置版本装，装好后仍可在运行面板里升级。已经装好的实例不受影响，升级仍走
原来「先备份再换版本」的流程。

**内置的 Hunter 版本升到 1.2.2**，离线或查不到最新版时装的也是 1.2.2。Hunter 1.2.2 修了
国内网络下新装后模型选择器为空、重装后界面卡在旧登录状态两个问题。

## [0.1.14] - 2026-09-24

迭代轮 I14。一句话：**0.1.13 在用户 Mac 上真跑了一遍，四处不对，这一版全修了。**

### 修复

**macOS 上的自更新原来是坏的（重要）。** `--self-update` 在 macOS 上会去下一个
**Linux 的 `.deb`**，然后报 `dpkg: command not found`。现在该下哪个包由
`latest.json` 里这台机器自己那一项说了算：macOS 取 `*_universal.app.tar.gz`，
验签后**就地换掉那个 `.app`**（`/Applications` 默认就写得了，通常一个密码框都不弹；
真写不了才走 macOS 自己的授权框）。Windows 取 `*_x64-setup.exe` 并以被动模式安装，
Linux 的 AppImage 与 `.deb` 两条路不变。换包失败时旧版本会被放回原位。

**「删除应用会腾出多少」原来虚高了大约 15 倍。** 体积现在按**实际占用的磁盘块**统计
（内置运行时那台虚拟机的磁盘镜像是稀疏文件，对外宣称几十 GB、实占一两 GB）；
「工作目录」与「运行环境」分开算，不再是同一个数字；
「不能同时删运行环境」那段提醒也不再连说两遍。

**重装时会沿用上次保留下来的 key。** 「只删除应用，保留数据」承诺过
「重新安装会直接沿用」，但装的时候还是要你重新填一次 key。现在要 key 之前先读
`~/.hunter/app/.env`：格式对、验得过就直接用，并说一句「沿用上次保留的 key」；
验不过才问你要，而且会说清楚为什么。命令行与界面都是这样。

**重装完成后，每天自动备份的定时任务会自己装回来 —— 而且这回说得出来。**
这件事原来就会做，但只写在日志里（而日志刚好在删除应用时被清掉了），
界面与命令行上一个字都没有。现在它是安装最后一步的一条结论：装上了、没装上（连原因）、
或者「你自己关掉了所以没装」。删除应用时也会提前说明「重装后会自动装回」。

**`.env` 里留着一个不像 key 的东西时，现在会说明白。** 上一条只处理了
「留着一把好 key」与「留着一把网关不认的 key」，漏了第三种：留着的那一项
被手改过、或者粘贴时少了一截。原来这种情况一声不响，你看到的只有
「没法交互式问 key」—— 就像它根本没去读过。现在会说清楚它是什么、为什么用不了。

### 其它

**Hunter 版本升级（`--upgrade`）第一次在真机上走完了全程。** 这条路一直有，
但从来没有在真实环境里验过。这一版验了三种走法：升上去（1.2.0 → 1.2.1，
含升级前自动备份与校验）、版本号根本不存在（如实报错，**不谎称已回滚** ——
因为那时还什么都没动）、以及拉不到镜像（配置整套写回、服务照常在跑，
你的数据一行没动）。

---

## [0.1.13] - 2026-09-23

迭代轮 I13。一句话：**你的数据有了退路，删除有了门槛，磁盘和内存出事了有人说一声。**

用户 2026-09-22 的原话里剩下的三件事，这一轮做完：

> 可以执行删除应用，但需要输入「删除应用」文字，避免用户错误点击按钮。删除前，还需要提醒，
> 数据库是否需要保留，还是只删除应用。同时支持本地数据库文件备份和恢复，可以设置自动备份时间、
> 备份目录……并可以分析硬盘、内存异常情况，给予提醒和智能分析解决。

### 变更

**每天自动备份，启动器关着也照样跑。** 默认每晚 00:00，保留最近 3 天
（按天保留，同一天多次只留最后一份；升级前的自动备份另外单独留最近 2 份，不占这个名额）。
备份目录按平台自动建议，**放在 Hunter 的工作目录之外** —— 删除应用永远波及不到它；
检测到外接盘时会额外建议一个位置（只建议，不自动选）。

一份备份里有：数据库（`pg_dump -Fc`，自带压缩）、**密钥卷**（少了它，加密过的配置解不开）、
自建技能、历史对话、`.env` 与两份 compose、以及一份 `meta.json`
（逐个文件的大小与 sha256、表数与**真数出来的**行数、校验结论）。

**做完立刻校验**：用 `pg_restore --list` 把转储的目录读一遍，读不出来就判这次失败。
一份「看起来在那儿、其实恢复不了」的备份比没有备份更糟。

定时靠的是**你自己账号下的**系统机制：macOS 用户级 LaunchAgent、Linux `systemd --user`
定时器（`Persistent=true`，错过会补跑）、Windows 任务计划（「错过后尽快运行」）。
全程不需要管理员密码，删除应用时会一并移除。Hunter 停着的时候只把 postgres 临时拉起来，
做完停回去；连运行环境都没起的话**记为「跳过」并在下次打开时提示**，不擅自把整套服务启动起来。

备份失败会在运行面板顶上出现红色横幅、「最近备份」那一行变红、托盘上也挂一条；
连着失败两次会把这件事交给诊断助手。

**恢复。** 运行面板 →「备份与恢复」→ 选一份 → **逐字输入「恢复数据」**。
恢复会先把现在的数据备份一份（挑错了还能再恢复回来），再停 api / opencode / web /
llm-shim、`pg_restore --clean --if-exists`、恢复密钥卷与 `JWT_SECRET`、起回来、等六个服务健康。
支持**从任意目录的备份恢复** —— 换电脑就是把那个文件夹拷过去，在新机器上指给启动器看。
备份来自更新的 Hunter 版本时会拦下来，先提示升级。

**删除应用。** 放在运行面板的「更多 ▾」里，三步：

1. 选范围。默认是**「只删除应用，保留数据」**：容器与启动器配置删掉，
   数据库、密钥卷、历史对话、自建技能全留着，重装直接沿用，**登录也不会失效**。
   另一档是「删除应用和全部数据」，会显示数据概况（多少张表、最近一次写入是什么时候），
   「删除前先备份一次」默认勾上。
2. **逐字输入**「删除应用」（选第二档是「删除应用和数据」）。差一个字，按钮一直是灰的。
3. 执行并逐条报告：删了什么、留了什么、腾出多少空间。

macOS 上数据卷在那台专用虚拟机的磁盘里，所以**「保留数据」那一档下「顺便删掉运行环境」是禁用的**
—— 删了它等于把数据一起删掉，界面上把原因写在旁边。**备份目录在哪一档下都不会被删。**

**硬盘、内存、服务异常的提醒。** 七类监测项，两级阈值（都在 `launcher.toml` 的 `[monitor]` 里）：
系统盘剩余、运行环境虚拟机磁盘、备份目录所在盘、这台电脑的内存压力、运行环境内存、
服务反复重启 / 被 OOM 杀掉、数据库单日增长。

出事了在运行面板顶上出一条横幅：**结论一句话，展开能看到它凭什么这么说**（全是实测数字）。
能自己动手的只有三件事 —— **只清本项目别的版本的镜像、没有任何容器在用的悬空数据卷、过期的备份**；
需要删你自己的文件、给虚拟机扩容、调内存的一律只给建议。规则层判不了的那几条
（为什么被 OOM、为什么反复重启）可以点「让 AI 帮我看看」，送出去的是**脱敏后的指标快照**
（只有数字与服务名，没有路径、没有主机名、没有 key）。同一个问题 24 小时内不重复打扰。

**「内存吃紧」那条提醒会先说清 Hunter 占了多少**：占比低于 20% 时它写的是
「卡的主要原因多半不在它」—— 免得用户把电脑卡的账都算到 Hunter 头上。

### 命令行

```
hunter-launcher --backup [--scheduled]              立刻备份 / 定时任务的无界面入口
hunter-launcher --restore <id|目录> --confirm 恢复数据 -y
hunter-launcher --schedule status|install|remove
hunter-launcher --uninstall-plan                    删除会动到什么（只读）
hunter-launcher --uninstall app|all --confirm <文字> -y
hunter-launcher --alerts                            硬盘 / 内存 / 服务异常（只读）
hunter-launcher --cleanup [-y]                      只清本项目旧镜像、悬空卷、过期备份
```

### 安全边界（新增的三道窄门，都是代码不是承诺）

| 要做的事 | 凭什么敢做 |
|---|---|
| 装 / 卸定时备份任务 | `argv_schedule`：只认**我们自己那一个** Label / 单元名 / 任务名，参数逐字写死，一条 `sudo` 都没有，碰不到 `/Library/LaunchDaemons` 与 `/etc/systemd/system` |
| 删本项目的数据卷 | 每一个都先过 `own_volume`（`com.docker.compose.project` 标签必须是 `hunter`，**查不到也不删**），再过 `argv_volume_rm`（不认 `prune`、不认任何开关） |
| 删本项目的旧镜像 | `argv_image_rm`：只认指名道姓的 `docker image rm`，而且**有任何容器在用它就跳过**。`image prune` / `system prune` 一律拒绝 |

模型走的那道通用门（`guard::argv`）**一个字都没放松** —— 它照样拒绝上面这三类命令。

### 已知的边界

* macOS 的 LaunchAgent 这条路**没有真机验证**：本项目只有一台 Linux 测试机。
  代码与单测写全了，等用户在自己的 Mac 上跑。
* Linux 上 `systemd --user` 的定时器默认只在用户有会话时活着。
  启动器**如实报告**这个状态，不替你开 `loginctl enable-linger`（那是一条会改变系统行为的命令）。
* 「托盘通知」这一版是**托盘 tooltip + 一个托盘菜单项**，不是系统级弹窗
  （和「启动器有新版本」那一条同一套做法）。理由见 I13 报告第六节。

---

## [0.1.12] - 2026-09-23

迭代轮 I12。一句话：**装好了就记住它。**

起因还是用户自己的 Mac（2026-09-22 23:10）：Hunter 六个服务 6/6 健康跑着、
网页 `127.0.0.1:3100` 返回 200，重新打开启动器看到的却是
「欢迎使用 Hunter 启动器」。根子在判据上 —— 那时候启动器只看
`~/.hunter/launcher.toml` 里的一行标记，**而那一行只在安装流程自己成功走完时才写**；
那一次安装在启动器看来是失败的，最后是在外面修好的，没有人回来把标记补上。

### 变更

**打开启动器先查一次现状，不再闪欢迎页。** 窗口一出来是一张「正在检查 Hunter 状态」，
不到一秒就跳走。判据改成**现状优先**：六个容器在就是装过，标记写的是什么都不影响 ——
而且启动器会顺手把标记补上，面板顶上说一句「检测到 Hunter 已在运行，已记录为已安装」。
容器没了、上一次的数据卷还在时，会先进一页「检测到上次的数据」。

配置里那条安装记录也记全了：哪一版启动器装的、装的哪一版 Hunter、用的哪套运行时、
**上一次真的看到 6/6 健康是什么时候**。运行面板右侧多了「安装于」「上次正常运行」两行。
（`[launcher] version` 那个从 0.1.7 起就没更新过的字段，现在并进 `[install] launcher_version`，
每次启动都写成当前版本。）

**运行面板上半部多了三层资源。** 这台电脑（CPU / 内存 / 内存压力 / 系统盘）、
Hunter 运行环境（虚拟机分配与实际用量）、数据（数据库 / 数据卷合计 / 镜像），
外加一行每个服务的 CPU 与内存。

分三层是有理由的：在 macOS 上 Hunter 跑在一台只服务它的虚拟机里，
「这台 Mac 内存 92%」和「那台虚拟机内存 92%」是两件不同的事。每张卡都写着数字从哪问来的。
**窗口看不见时全部停止采集**（这几条命令要起子进程，没人看的时候采它只是费电）；
拿不到的每一项都显示 `—`，鼠标停上去能看到原因。

**停止 / 启动 / 重启补全了。**

* 停止：先问一句。用内置运行时的机器上多一个默认勾上的选项
  「顺便把 Hunter 的运行环境也停了（释放约 4 GB 内存）」—— 不勾的话那台虚拟机还占着内存；
* 启动：运行环境没起的话先把它起起来（顺带检查一次虚拟机的 DNS），再起容器等健康；
* 重启：可以六个一起，也可以**只重启一个**（容器不换，端口映射与数据卷都不动）。

操作过程中按钮置灰，下面一条条显示刚做过的事。托盘上那三项走同一条路。

**电脑或运行环境重启之后，容器会自己回来。** 覆盖文件给六个服务都写了
`restart: unless-stopped`；万一还是没回来（比如内置运行时的虚拟机停过一次），
**打开启动器就会自动拉起**，不需要点任何按钮。
你自己点过「停止」的那一次不算 —— 启动器记着那是你的决定。

**重装之前先看一眼有没有旧数据。** 安装流程在写配置之前多了一步（只读）：
问一遍 docker 有哪些数据卷、读一下数据库的大版本、看一眼 `JWT_SECRET` 还在不在。

数据库 + 密钥卷 + `JWT_SECRET` 都在、版本不比要装的这一版新时，**直接沿用** ——
不删卷、不重建库、不重新生成密钥，也不会掉登录。
唯一会停下来的一种情况是**你的数据比要装的这一版新**（新错误码 `E_DATA_DOWNGRADE`）：
硬装会把数据弄坏而且没法撤销，所以停在动手之前。

**命令行新增三条只读命令**：`--boot-state`（打开启动器时那一次判定）、
`--monitor`（三层资源）、`--data-check [--deep]`（有没有上一次的数据）。

---

## [0.1.11] - 2026-09-23

迭代轮 I11。一句话：**已经装好了就别再装。**

起因还是 0.1.9 在用户自己的 Mac 上。21:39 报了「启动超时」进错误页；
22:05 根因在外部被修好，六个服务全绿、网页返回 200 —— 而界面一直到 22:50
还挂着 21:39 那张失败卡片。用户在那张卡片上唯一能点的是「重试」，
点下去的结果是：重新取 compose、重写 `.env`、**开始重新下载 849 MB**。

### 变更

**错误页现在会自己看现状。** 进了错误页之后，启动器每 20 秒只读地问一次
「Hunter 到底在不在跑」（`docker compose ps` + 一次本机网页请求）。
一旦六个服务全就绪、网页也打得开，界面**自己回到运行面板**，
并用一句人话说明刚才发生了什么。AI 或固定规则每跑完一个动作也会立刻重查一次。

**「重试」先看一眼，再决定要不要装。** 点重试的第一步是只读探测，然后分三种走法：

* 那一套已经在正常跑 → **什么都不做**，直接告诉你「Hunter 已经在运行」；
* 只有个别服务不正常 → **只动那几个**（起它 / 重启它），
  不重新取配置、不重新下载镜像、健康的容器一个都不重建；
* 这台机器上确实没有一套能用的 → 才走完整安装。

启动器自己上一次装的那一套，永远是复用或修好，**不会再并出第二套**占你的盘和内存。
（「本机已有另一套 Hunter 就并存换端口」那条规矩只对**你自己另外装的**那些成立。）

**错误页多了一个「关闭」。** 它只是把窗口收回托盘：不退出、不停容器、不动任何服务。
复查说没异常的时候，它就是那一页的主按钮 —— 那种情形下「重试」本来就是错的那个选择。

**端口那一栏不再骗人。** 诊断信息里的端口现在分三类显示：空闲 / 被 Hunter 自己占用 /
被其他程序占用。0.1.9 那份用户导出的诊断里，五个正被 Hunter 自己占着的端口
全印着「空闲」—— 送给模型的前提一错，后面的推理全是白做的。

**手工敲 `docker compose` 也不会把端口开到局域网上。** `~/.hunter/app/.env` 里
现在写着 `COMPOSE_PROJECT_NAME` 与 `COMPOSE_FILE`，在那个目录直接
`docker compose up -d` 会自动带上启动器的覆盖文件（端口只绑 127.0.0.1）。
启动器每次启动还会对一遍：**跑着的容器**绑在哪，和配置说的一不一样 ——
不一样就按配置把那几个容器收回本机。

（背景：2026-09-22 22:01 有人在这台 Mac 上手工跑了一次 `docker compose -p hunter up -d`，
漏了覆盖文件，五个端口在局域网里可达了约 50 分钟。）

---

## [0.1.10] - 2026-09-22

迭代轮 I10。这一版只有一条主线：**Hunter 自己那台虚拟机从出生起就没有 DNS。**

起因是 0.1.9 在用户自己的 Mac 上真机运行。该做的都做对了 ——
认出上次装剩的东西、走你自己配的代理取到 compose、从腾讯云香港拉下 849 MB、
起容器成功、全程一次点击都不用。然后卡在最后一米：opencode 十三分钟不健康，
报了一句「启动超时」，而确定性规则认不出来。

真正的原因用日志永远看不出来：**启动器自带的那份虚拟机系统镜像里没有
systemd-resolved**（Ubuntu 24.04 把它拆成了单独的包，minimal 镜像不装），
而镜像里那条 `/etc/resolv.conf` 符号链接照旧指向它 —— 链接在，被链接的人不在。
于是那台虚拟机、以及它里面的每一个容器，**解析不了任何域名**。

比健康检查更要紧的是：容器连不上模型网关 —— **就算六个服务全绿，你也一句话都问不出来。**

### 变更

**虚拟机现在一定有 DNS。** 启动器会在**它自己那台虚拟机**里把 `/etc/resolv.conf`
写好（lima 的网关 + 两个国内可直连的公共 DNS，数值都是实测挑的），
并装一个开机重写它的服务，所以虚拟机重启、宿主机重启之后仍然有效。
每次启动器启动、每次安装走到「检查 Docker」那一步，都会重新解析一次域名来复查 ——
**不假设上一次修好了**。

**「容器能不能上网」成了一项必检。** 镜像拉完之后，启动器会用刚拉下来的那个镜像
起一个一次性容器（用完即删、不带你的 key），从容器里真的连一次模型网关。
连不上就不算装完 —— 因为一个起得来却问不出话的 Hunter，对你没有意义。
过程流里能看见这张卡片和它的真实耗时。

**已经装好的机器会自己痊愈。** 0.1.10 一起来就会检查那台虚拟机的 DNS：
已经好的什么都不动（包括你自己手工改过的），还坏着的自动修好、
让已经在跑的容器重新拿一份 DNS，然后把没装完的安装接着往下走。

**两个新错误码**：`E_RUNTIME_NO_DNS`（虚拟机没有可用的 DNS）、
`E_CONTAINER_OFFLINE`（容器连不上模型网关）。两个都有对应的确定性规则，
认出来是零额度消耗的；跑在你自己装的 Docker 上而我们确实修不了的那一类，
会如实说清楚往哪儿查，而不是给一个没用的按钮。

**命令行新增 `--check-net`**：一条命令查完「虚拟机有没有 DNS」和
「容器连不连得上模型网关」，两项都通才返回 0。

### 边界没有变

启动器**不会**改你电脑的 DNS、hosts、代理或防火墙设置。
这一版新增的写文件动作只作用于 Hunter 自己那台虚拟机（`colima-hunter`），
而且只认一张逐字写死的命令表 —— 这是写在程序里的，不是一句承诺。

---

## [0.1.9] - 2026-09-22

迭代轮 I9。这一版只有一条主线：**「上次没装成功」不该让这次更糟。**

起因是 0.1.8 在用户自己的 Mac 上第一次真机运行。那台机器上留着 0.1.7 装了一半的
残骸（`~/.hunter/runtime` 里的 docker 与 colima 都在，虚拟机从没起来过），
**0.1.8 把自己上一次留下的东西认成了「你装的 Docker / Colima」**，于是：

- 「没有 Docker → 装一套内置运行时」这条主线一步都没走到；
- 它执行了一条不带 profile、不带 `COLIMA_HOME` 的 `colima start`，
  **在用户家目录里建出了一台和 Hunter 毫无关系的虚拟机**（占 1.4 GB）；
- AI 后来把虚拟机起起来了、docker 也能用了，**安装却没有接着往下走**，
  界面停在「没能自动装好」；
- 那段时间里所有端口检查都问错了 Docker，端口冲突检测整条被跳过。

### 变更

**上次装剩的东西，这次认得出来了。** 启动器现在先问「`~/.hunter/runtime` 里那一套
是什么状态」，再问「你自己的 Docker 在不在」。文件缺几个就补齐缺的那几个
（已经下好并核过校验和的一个字节都不重下），虚拟机没起来就起它，起不来就
**清掉那台虚拟机重建一次**再起。新的错误码 `E_BUILTIN_DOWN` 专门指这件事，
不再和「你的 Docker 没开」混为一谈。

**不会再碰你的 `~/.colima`。** 启动器发出的每一条 colima 命令都必须带指向
`~/.hunter/runtime` 的 `COLIMA_HOME` 和 `--profile hunter`；缺一样就在**真正执行之前**
被拒掉并记进审计日志。你自己装的 colima 只会被「启动它原本就有的 profile」，
绝不会替你新建虚拟机。

**修好之后会自己接着装。** 诊断助手（规则层或 AI）任何一步让 Docker 变得可用，
安装就自动从写配置继续往下走，不再停在失败卡片上。

**全自动档下不再让你点确认。** 你在授权页上选的是全自动，那么出问题之后
那些安全动作（换端口、重试、启动运行时……）由启动器自己执行 ——
把关的门一道没少，只是不再让你去按那个按钮。**做了什么依然看得见**：
界面上多了一块「启动器已经替你做了这几步」，一条条列出来。

**端口检查问的是正确的那个 Docker。** 用内置运行时时，所有 `docker ps`
都指向它自己的 socket；一次检查只采一遍现场（原来是五个端口各采一遍）。

### 修复

- **我们的残骸会把你一套好好的 Docker 弄坏**：`DOCKER_CONFIG` 原先只要
  `~/.hunter/runtime` 里有东西就指过去，于是残骸里那个坏掉的 compose 插件被用上，
  安装在「写配置」那一步失败。现在谁生效就用谁的配置。
- 自动修复时「这个办法没成」被当成了「没有别的办法」，第二条路一次都没机会跑。
- 一个计划的头一条动作失败过之后，后面那条「等它生效」会被单独拎出来干等到超时，
  而界面上说的还是头一条要做的事。
- 命令行上打出过 `E_UNKNOWN: Hunter 自己那台虚拟机没起来` 这种自相矛盾的话
  （错误码清单抄了三份，加新码时漏改一份）。
- `E_DAEMON_DOWN` / `E_WSL_MISSING` 两条错误提示里还写着让你去敲命令，已改写。

### 已知限制

- **macOS 未真机验证**（本项目只有 Linux 测试机）。mac 分支的代码路径已逐行审阅，
  CI 在 macOS 上跑单测并打 dmg。
- 0.1.8 如果已经在你的 `~/.colima` 里建出过一台叫 `default` 的虚拟机，
  **启动器不会替你停、也不会替你删**（那是你家目录里的东西）。
  常见问题里「之前装过没成功，再装会怎样」一节写了它是什么、占多少。

---

## [0.1.8] - 2026-09-21

迭代轮 I8。这一版只有一条主线：**把「让你自己动手」的地方全部删掉。**

起因是 0.1.7 在用户自己的 Mac 上第一次真机运行 —— 四个组件从腾讯云香港装好了，
虚拟机起不来，最后停在一张卡片上：

> 请在终端里执行：`brew install --cask orbstack`（或者去 orbstack.dev 下载）
> 　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　［我执行完了，继续］

用户的原话是：「很多用户连这个都不明白」。于是这一版把这类出口从代码里清掉了。

### 变更

**安装过程中你一次都不用点。** 输 key → 授权页点一次「开始安装」，到六个服务健康为止
界面上不再出现任何需要你决定的按钮（「停止安装」那个入口留着）。原先会停下来问你的
三件事，现在启动器自己按最佳方案办，并在过程流里用一句人话说明做了什么决定、为什么：

- 要装新软件 → 按「最不打扰你」的顺序依次试（见下）；
- 这台电脑上已经有另一套 Hunter → 自动「两套并存、换一组端口」，你原来那套一点都不动
  （想改成直接用已有那套，设置页里切换）；
- 这次的额度用完了 → 不问你，改成只按固定规则继续；真装不上就自动生成脱敏诊断包。

「每一步都问我」那一档还在，挪到了**设置页 → AI 助手**，默认不再是它。

**没有 Docker 时三条路自动依次走完**，每一条都是启动器自己执行：

1. 内置运行时（装在 `~/.hunter/runtime`，不用密码）
2. OrbStack 官方安装包（下 dmg → 验苹果签名与公证 → 装 → 打开 → 等 docker 就绪）
3. Homebrew —— **这台电脑上没有 brew 就先替你把 brew 装上**，再 `brew install --cask orbstack`

排序原则是「同样能解决问题时，优先不需要任何交互的那条」，所以要密码的路线排在最后。

**需要管理员权限时弹的是系统自带的密码框。** 之前的承诺是「绝不使用管理员密码」，
代价是撞上要 root 的事只能把活儿丢回给你。现在改成：需要时由 macOS / polkit / UAC
**自己**弹授权框，弹之前界面先用一句人话说清「要做什么、为什么要密码」。
启动器不自绘密码框、不保存密码、不把密码写进日志。**其余禁止项一条没放松**：
不删你的文件、不改你的网络与安全设置（代理 / DNS / hosts / VPN / 防火墙 / Gatekeeper / SIP）、
不动别的项目的容器与数据、不执行模型自己编的命令。

Linux 上的启动器自更新跟着改了：`.deb` 以前是「包下好了，这条命令你自己去敲」，
现在是下好包 → 弹 polkit 的原生授权框 → 启动器自己 `dpkg -i`。

**开源协议改为 Apache License 2.0**（原为 MIT）。新增 `NOTICE` 与
`THIRD_PARTY_NOTICES.md`；启动器运行时下载的那些第三方程序
（Colima MIT、Lima Apache-2.0、docker CLI 与 compose Apache-2.0、Homebrew BSD-2-Clause、
OrbStack 专有）**不随安装包分发，各自保留各自的许可证**，不因本项目换协议而改动。

### 修复

**虚拟机系统镜像现在由启动器自己下。** 这是 0.1.7 在用户 Mac 上卡住的直接原因：
`colima start` 会自己直连 GitHub 去取 `colima-core` 的
`ubuntu-24.04-minimal-cloudimg-*-docker.raw.gz`（341 MB / 316 MB），那一次下载不受我们控制，
用户那台机器上直连 GitHub 十秒超时。现在这两个文件进了内置运行时的清单：
GitHub 与腾讯云香港并列择优、版本与 sha512 写死、校验不符拒绝用、下载有真实进度，
下好之后用 `colima start --disk-image <本地文件>` 交给 colima，它一个字节都不用自己下。

**沿用你自己配好的网络代理。** 用户那台 Mac 的系统设置里配着 `127.0.0.1:7897`，
直连 github.com 十秒超时、走代理两秒就通 —— 路是铺好的，是我们没走。现在启动器在
直连失败时会**只读地**读出这台机器上的代理（macOS `scutil --proxy`、Windows 注册表的
Internet Settings、Linux 环境变量），用它重试，并把 `HTTP_PROXY` / `HTTPS_PROXY` /
`ALL_PROXY` / `NO_PROXY` 传给 colima、limactl、docker，以及（用 `colima start --env`）
虚拟机里的 docker。界面上会说一句「检测到你设置了网络代理，已沿用」。
**只读取，绝不修改**你的网络设置。

**「已自动解决 N 个问题」现在如实计数**：自动换下载源、自动改走代理、自动换兜底路线，
每一次都算一个，并各自在过程流里留一行说明。

### 内部

- 新增一条流水线检查 `scripts/check-wording.py`：扫界面文案、规则层文案、动作表描述，
  出现「请在终端里执行」「复制这条命令」「我执行完了」「请你选择」「点一下继续」这类
  要用户自己动手或替我们做决定的字样，CI 直接红。它自带 9 条反例 + 8 条正当文案的自测，
  证明规则本身不是空跑。**这条原则靠代码守，不靠提示词。**

---

## [0.1.7] - 2026-09-21

迭代轮 I7。设计文档 §十分期里原定 I6 的四项 —— 上一轮被 macOS 的 P0 抢占，本轮补齐。

### 新增

**没有 Docker 也能零点击装完（macOS）** —— 以前「这台电脑没装 Docker」要你自己去下载、
拖进应用程序、点开、过欢迎页。现在授权页上多了一项**默认勾着**的勾：

> 如果电脑上没有 Docker，允许 AI 为你安装（装在 `~/.hunter/runtime` 里，不改系统，可一键卸载）

勾着的话启动器装一套**完全用户态**的运行时到 `~/.hunter/runtime`：
Colima v0.10.3 + Lima v2.2.0 + docker 客户端 29.8.1 + compose v5.5.1。

- **版本与 sha256 写死在程序里**，下完逐个核对，对不上就删掉不用 —— 不跟 `latest` 漂移；
- 两个下载源并列测速：GitHub / docker.com 与腾讯云香港；
- **不要管理员密码、不改系统任何地方**：不写 `/usr/local`、不改 shell 配置、不碰你的 `~/.docker`。
  虚拟机、socket、配置全在 `~/.hunter/runtime` 里；
- 本机已经有 OrbStack / Docker Desktop / Colima 的话**一个字节都不下**（在跑就直接用，
  装了没跑就先把它点起来）；
- 设置页 →「容器运行时」→「卸载内置运行时」一键清干净，Hunter 自己的数据卷不受影响。

设置里另有一条备选路线「OrbStack 官方安装包」（下 dmg + 验苹果签名与公证 + Team ID）。
它装得更快，但**首次打开 OrbStack 时 macOS 与它自己会弹欢迎页、可能要管理员密码** ——
那是系统与 OrbStack 的交互，启动器不会替你点，所以它不是默认。
（OrbStack 个人使用免费，商用需另外授权。）

> Windows 装 WSL / Docker Desktop 要管理员权限，Linux 装 Docker Engine 要 sudo ——
> 这两条路启动器**不替你提权**，只把要敲的命令原样写给你，你执行完点一下就接着装。

**直接用你已经装好的那一套 Hunter** —— 发现本机已有别的 Hunter 时，过程流里的卡片
给两个按钮：「和它并存（推荐，不动它）」与「直接用它，不再装一套」。
**这张卡片不拦着安装**（并存本来就是默认），点了才改道。

选「直接用它」之后运行面板变成「正在管理你原有的 Hunter」：看容器状态、看日志、打开网页。
**停止 / 启动 / 重启每一次都要你再确认一遍**；启动器不会改它的配置，
**任何情况下都不删它的数据卷**（动作表里根本没有对它 `down` / `-v` 的路）。
读不到它的 compose 文件时如实降级成只读监控。

**复核员** —— 计划里出现「需要你同意」级别的动作时，启动器会再问一次模型：
换一段完全独立的提示词、不带上一轮的对话历史、不给它任何工具，只判两件事 ——
「会不会伤到你已有的数据 / 网络 / 其他项目」「说的话对不对得上现场证据」。
过程流里单独一张「复核」卡片。否决就退回去重想（带着否决理由）。

> **复核通过不等于可以执行。** 守卫（程序里那道硬校验）与你的确认这两道照样要过。
> 多一个模型把关，不是让代码那道门松一松的理由。

**发送诊断给开发者** —— 修不好时的最终页新增一个按钮：在你自己的机器上生成脱敏诊断包 →
把**将要贴出去的 issue 标题与正文原样摆给你看** → 你点了才打开浏览器（内容已预填）。
诊断包**不会自动上传**，本地路径一并给出。出口闸这一版连**用户名与主机名**都扫，
扫到就不给「打开 GitHub」那个按钮。

### 变更（会影响现在的用法，请先看这一条）

**Hunter 的网页现在只有装它的那台电脑能打开。**

六个服务的端口全部绑在 `127.0.0.1` 上（以前只有 api / opencode / postgres / redis 是，
网页端口绑的是所有网卡）。手机、平板、同一网络里的另一台电脑**都打不开**了。

为什么改：Hunter 的聊天界面不需要任何口令。以前只要能连到你这台机器，
打开 `http://<你的 IP>:<端口>` 就能直接用，烧的是**你的**额度 ——
这件事从 0.1.0 起就是这样，我们在 0.1.2 把开关做了出来，但默认值一直没动。

**局域网访问（让同一网络的其他设备也能打开）是付费版功能**，免费版里没有这个开关：
设置页「谁能打开 Hunter」那一栏现在是一行只读说明，
`launcher.toml` 里手写 `web_bind = "all"` 或 `"0.0.0.0"` 也不会生效 ——
覆盖文件照样按 `127.0.0.1` 生成，并在日志里记一条说明它被忽略了。

**从旧版本升上来的机器不受影响**：升级**不会**悄悄改动你已有的配置
（不然「昨天手机上还能开」会在你不知道的情况下突然失效）。
运行面板顶上会出现一条提示，想收紧就点「只允许本机访问」。
**收紧是单向的**，收完不能再放开。

### 改进

- **预检阶段自动换端口现在算「已自动解决 1 个问题」**。0.1.6 在 Mac 上换掉了 5 个被占的
  端口、装成功了，总览那一行却写「已自动解决 0 个」—— 用户看见它发现问题、处理掉了，
  那就该算数。
- **事件流按次归档，保留最近 5 份**（`~/.hunter/logs/assist-events-<时间戳>.jsonl`）。
  以前一开新的就把上一次覆盖掉，而排查「上一版装到哪一步失败的」要的正是那一份。
- **「找不到 Docker」时规则层先自己按已知位置硬找一遍**，找到就直接写进设置。
  0.1.5 实测这一步交给模型花了 8,632 token，而且它第一轮还要求再探一次我们刚探过的东西。
- **命令行下的「逐步确认」档第一次真的能用了**：stdin 是终端就把问题打出来等你输 y/n；
  是管道 / CI 就照旧按「不」处理并在开头说清楚。
- 命令行新增 `--takeover list|use|status|logs|stop|start|restart|release`、`--feedback`、
  `--review`。`--takeover` 的改动类子命令要 `-y`——那是二次确认在命令行下的形态。

## [0.1.6] - 2026-09-21

迭代轮 I6。0.1.5 在用户 Mac 上真机验收，端口那一条修好了，却卡在下一步：
**`docker compose pull` 一个镜像都拉不下来**，而且界面上那句「原话：」后面是空的。
本轮是一次热修，原定 I6 的内容（复核员、`install_runtime` 等）顺延到 I7。

### 修复

**Docker 找不到它自己的凭据助手（P0）** —— 从访达 / 程序坞双击起来的启动器，
PATH 只有 `/usr/bin:/bin:/usr/sbin:/sbin`。I4 已经让**启动器**按已知位置找到了 docker，
但 **docker 自己**去 `$PATH` 上找 `docker-credential-osxkeychain` 时，
拿到的还是那份受限 PATH：

```
error getting credentials - err: exec: "docker-credential-osxkeychain":
  executable file not found in $PATH, out: ``
```

（用户的 `~/.docker/config.json` 里有 `"credsStore": "osxkeychain"`，
是 OrbStack / Docker Desktop 装的时候自己写的；助手在 `/usr/local/bin`。）

现在**所有** docker / compose 子进程都用同一套环境：
`PATH` = 原 PATH + docker 所在目录 + 它软链指向的目录 + 平台已知位置
（和找 docker 用的是同一份清单，去重、保序、只加真实存在的目录），
日志里记一行「补了哪些目录」。Linux 与 Windows 同样适用。

**补全之后仍然找不到助手时**，启动器在 `~/.hunter/docker-config/` 里另起一份
**不带 `credsStore` 的配置**给自己用（Hunter 的六个镜像都是公开的，本来就不需要登录）。
**你的 `~/.docker/config.json` 一个字节都不会被改** —— 这不是承诺，是路径守卫拦得住。

**「原话」不再是空的** —— compose v5 把顶层错误写成
`{"error":true,"message":"…"}`，而 0.1.5 的解析只认另外两种形状，
于是失败原因整段丢掉了，用户和 AI 都只看到「退出码 1。原话：」。
现在三种 JSON 形状都认，stdout 与 stderr 都收；真的一个字都没输出时，
也如实说「一个字的错误都没有」，而不是留半截话。

**不再在两个镜像源之间来回兜圈** —— 新错误码 `E_CRED_HELPER`：
本机凭据助手缺失是**本机配置**的问题，与下载源无关，换源永远修不好。
0.1.5 在用户机器上为此换了三次源、花了 268 秒、一个问题都没解决。
另外加了一条通用闸门：**同一条失败原话只换一次源**；换完原话一个字都没变，
就判定「与源无关」，把现场连同原话交给诊断员，而不是接着换。

### 测试

测试机上用**真的 0.1.5 二进制**复现了这次失败（受限 PATH + 一个找不到的 `credsStore`），
再用新版跑同一个现场：

| 现场 | 0.1.5 | 0.1.6 |
|---|---|---|
| 助手在已知位置（`/usr/local/bin`） | 失败 · 4 回合 · 换了 3 次源 · 「原话」是空的 | **零点击到 6 / 6，0 个修复回合，69 秒** |
| 助手在任何已知位置之外 | 同上 | **零点击到 6 / 6，1 个修复回合，0 token，51 秒**（自动另起一份配置） |

两次跑完，用户已有的那套 Hunter 六个容器 ID 未变、数据卷清单与创建时间未变、
`~/.docker` 未被改动。

---

## [0.1.5] - 2026-09-21

迭代轮 I5。用户在自己的 Mac 上装 0.1.4，启动那一步报「端口已被占用」——
根因是端口探测在 macOS 上把**被占死的端口判成了空闲**。本轮先把这个确定性缺陷修掉，
再把整个安装改成「一次授权，AI 自动干完，过程实时给你看」。

### 修复

**macOS 上把被占用的端口判成空闲（P0）** —— 用户电脑上早有一套 Hunter 占着
3100 / 8100 / 3921 / 5442 / 6479，启动器只把 web 3100 改成了 3101，其余四个照原样用，
`docker compose up` 随后报 `Bind for 0.0.0.0:8100 failed: port is already allocated`。

根因：`std::net::TcpListener::bind` 在 Unix 上**默认给监听套接字开 `SO_REUSEADDR`**，
macOS 的 BSD 语义下这个选项会让「绑 `127.0.0.1:P`」在「`*:P` 已被别人占着」时照样成功。
web 探的是 `0.0.0.0` 所以查对了，其余四个探的是 `127.0.0.1` 所以全错。
Linux 内核的行为不同，所以 I4 在测试机上四个场景全过、一上真机就炸。

现在端口探测走**三重确认**，任何一路说「占用」就算占用，并记下**占用者是谁**：

1. 用 `socket2` 显式构造套接字、**不设 `SO_REUSEADDR`**，`0.0.0.0` / `[::]` / `127.0.0.1` 各绑一次
   （`127.0.0.1` 那一条是给 Windows 的：那边的 `SO_REUSEADDR` 允许抢占，通配绑定不会因为「别人占着回环」而失败）；
2. `docker ps` 的全部已发布端口 + 所属 compose 项目名（Docker 是端口这件事的最终裁判）；
3. `lsof -nP -iTCP -sTCP:LISTEN`（Windows 用 `netstat -ano`）补充占用进程名。

**起容器前的预检闸门** —— `docker compose up` 之前拿合并后的端口表再对一遍现场，
有冲突就先换端口，不等 Docker 报错。

**端口冲突有了自己的错误码 `E_PORT_CONFLICT`** —— 0.1.4 把
`port is already allocated` 归成了「启动超时」，于是规则层认不出来、AI 拿到的前提就是错的。
顺带补齐了 `no space left` / `pull access denied` / `TLS handshake timeout` / `i/o timeout`
的归类。

**认得出你电脑上已有的其他 Hunter** —— 镜像名含 `hunter-community-` 的其他 compose 项目
会被报出来（连同它用官方镜像的 postgres / redis），默认**并存换端口**，
界面上写清楚「启动器不会停它、不会删它」。

**重试前清掉本项目的 `Created` 残留容器** —— 不带 `-v`，删之前逐个核对 compose 项目标签。

### 新增

**一次授权 + AI 自动驾驶安装** —— 向导变成
「欢迎 → 输入 key → 一次授权 → 自动安装 → 完成」。
授权那一下之后**不需要再点任何按钮**（测试机实测 54–94 秒到 6 / 6 健康），
除非撞上三种必问情况：要装新软件、要动你已有的 Hunter、要超本次额度上限。

三档授权（设置页随时可改）：`auto` 自动驾驶（推荐）/ `confirm` 逐步确认 / `off` 关闭。

**实时过程流** —— 每一件事一张卡片，写人话；右侧「详情」里是命令、原始输出、耗时、token。
顶上一行常驻：现在在干什么、自动解决了几个问题、花了多少 token、第几个修复回合。

**AI 绝不会做的事，写在代码里** —— 路径 `canonicalize` 之后必须落在 `~/.hunter/` 内、
删除只限启动器自己生成的文件、docker 操作强制 `--project-name hunter` 并在执行前核对
容器的 compose 项目标签、没有任何需要 `sudo` 的动作、`volume rm` / `prune` / `down -v` /
改代理 DNS hosts 防火墙 / 任意 shell 一律不在动作表里。
每一次动作（**包括被拒绝的**）都写 `~/.hunter/logs/assist-audit.log`，设置页里能直接看。

**要管理员权限的事会请你出手** —— Linux 上 `systemctl start docker` 需要 root 时，
界面给一张卡片写明「请在终端里执行 `sudo systemctl start docker`」，
**不替你提权、也不假装做过了**。

### 其他

- `hunter-launcher --auto [--assist-mode auto|confirm|off]`：命令行下跑同一套自动安装，
  事件流同时落到 `~/.hunter/logs/assist-events.jsonl`。
- 慢机器上健康检查的等待上限可以调长（`[hunter] start_timeout_secs`，180–900 秒）。
- 下载那一步现在区分「真的下载了多少」与「本机已有多少」——
  镜像已在本机时如实写「这次一个字节都没下载」，不再拿 manifest 大小冒充下载量。

---

## [0.1.4] - 2026-09-20

迭代轮 I4。用户在自己的 Mac 上第一次真机跑 0.1.3，当场撞出一个 P0；
本轮修掉它，顺带把安装排障做成了两层的诊断助手。

### 修复

**macOS 上装着 Docker 却报「没有找到 Docker」（P0）** —— 用户的 OrbStack 好好装着、
终端里 `docker version` 能返回 Server 29.4.0，启动器却停在
`E_DOCKER_MISSING · 命令行里找不到 docker`。

根因和「装没装」毫无关系，是 **macOS GUI 程序的固有行为**：从访达或程序坞启动的程序，
拿到的 PATH 只有 `/usr/bin:/bin:/usr/sbin:/sbin`（`launchctl getenv PATH` 是空的 →
走系统默认），**不含 `/usr/local/bin`** —— 而 docker 恰恰就在那里
（一个指向 `/Applications/OrbStack.app/Contents/MacOS/xbin/docker` 的软链）。
终端里能跑，是因为 shell 读过 `/etc/paths` 与 `~/.zshrc`，GUI 程序读不到那些。

现在不再只看 PATH：按「配置指定 → PATH → 平台已知位置」的顺序挨个探，
找到第一个能执行的就用。Docker 检测页上直接写出**实际用的是哪一条路径、从哪找到的**
（是软链还会写出它指向哪里）；一个都没找到时，把探过的每一个位置和结果整份列出来。
mac 上探的位置包括 `/usr/local/bin`、`/opt/homebrew/bin`、`~/.orbstack/bin`、
`~/.docker/bin`、Docker.app 与 OrbStack.app 的 app 内目录、`~/.colima/bin`、`~/.rd/bin` 等；
Linux 与 Windows 各有自己的一份。装在清单之外的，可以在
`~/.hunter/launcher.toml` 的 `[runtime] docker_path` 里指一次。

`docker compose` 也一样：先试 CLI 插件，没有再找独立的 `docker-compose`。
之后**所有**调用 docker 的地方都走这条解析出来的绝对路径，不再有「有的地方用 PATH、
有的地方用绝对路径」这种事。

### 变化

**向导顺序调整：欢迎 → 输入 key → Docker → 选模型 → 拉镜像 → 启动。**
key 挪到了 Docker 前面。Docker 那一步是整个流程里最容易卡住的，而新加的 AI 诊断助手
要用这把 key 调网关；填 key 本来就只需要联网、不依赖 Docker，挪到前面没有代价。
命令行 `--headless` 的步骤号同步调整。

### 新增

**AI 诊断助手。分两层，顺序不能反。**

第一层是**确定性规则**，一直在跑，零延迟、零 token、不联网：多路径探测、
「装了但没起来」（OrbStack / Docker Desktop / Colima / systemd 分别判「装没装」与
「进程起没起」，直接给一个「启动它」的按钮）、端口占用、磁盘不足、拉取失败。
规则认出来的情况到此为止，一个字都不会发出去。

第二层是 **AI 兜底**，只有规则认不出来、或者你主动点了「让 AI 帮我看看」才会调。
它走 Hunter 网关（`hunter-chat`），用的就是你自己那把 key。几条边界：

* 送上去的是一份**结构化诊断**，你可以逐字展开看、整份复制走。里面没有 key、
  没有 `Bearer` 头，路径里的用户名一律换成 `<用户目录>`；发之前还有一道闸，
  整份再扫一遍 key 的形状，命中就干脆不发。
* 模型**不能**让启动器跑任意命令 —— 它只能从一张写死的动作表里挑，表外的一律拒绝
  并在日志里记一条（界面上也会显示「模型提了什么、为什么没执行」）。
  只读动作自动执行；会改动你机器的**必须先把完整命令原样给你看**，点确认才跑。
* 来回最多 3 轮，之后明说搞不定并给手工步骤。
* 没填 key、离线、网关不可用、额度用尽（402）、限流（429）、超时 ——
  任何一条都退回第一层，并在界面上写清是哪一条挡住了。
* 设置页有开关，**默认开**；关掉之后只剩第一层。

命令行也有：`hunter-launcher --diagnose` 只跑规则层（零 token），
加 `--ai` 才会真去问模型（显式开关，因为 `--diagnose` 常被写进脚本，
不能在你没要求的时候悄悄花掉额度）。

**诊断包与日志里的路径不再带用户名** —— `/Users/zhangsan/.hunter/app/.env` 会写成
`<用户目录>/.hunter/app/.env`，后半截留着（排查还得靠它）。诊断包是要贴进 GitHub issue 的。

---

## [0.1.3] - 2026-09-20

迭代轮 I3 的补丁版。又做了一次全量回归（清空 `~/.hunter`、用 0.1.2 的 deb
从零装、命令行与界面各一次、真实对话跑通），修掉本轮自审与回归撞出来的几条。

### 修复

**额度用完了，运行面板上一个字都没提** —— 这是本轮回归当场撞上的：测试 key 的
今日额度在回归途中真的用完了（网关 `remaining` 为 0），Hunter 里的对话被挡住，
而启动器的运行面板只摆着「304,144 / 300,000」两个数字，进度条还是正常的琥珀金。
输入 key 那一页白纸黑字写着「额度用尽会明确提示，不会静默降级」—— 装完之后那句话
其实没有兑现。现在额度用完时面板上会有一条明确的提示（用了多少、什么时候重置、
想现在就用该怎么办），额度卡的副行写「已用完 · 明天 00:00（上海）重置」，
进度条转成警示色；`hunter-launcher --status` 也一样。

**Podman 被误判成「Docker 装了但服务没起」** —— 本轮在测试机上真装了一次
podman 4.9.3 验这件事。它的 `docker version --format json` 是**合法的 Docker 形状
JSON**（只有 Client 没有 Server），而且整段里一个 `podman` 字样都没有，
所以原来那条「输出不是 JSON 才认 podman」的判断永远轮不到 —— 用户明明装着能用的
podman，看到的却是「连不上 Docker 后台服务」加一整页装 Docker 的指引。
现在认得出来了，并且**明说自己没验过**：Podman 仍是实验支持，整套安装从来没有在
它上面跑通过，`docker compose` 在 Podman 上要另外装 compose provider。

**更新页的卡片标题自己打自己** —— 标题写着「启动器有新版本」，正下方写着
「启动器已经是最新版」。标题现在跟着状态走。

**更新页的发布时间原样显示成 `2026-09-19T09:47:29Z`** —— 现在换算成上海时间
「2026-09-19 17:47（上海）」，和界面上别处一致。

**`--tray-menu` 打错一个字母会「成功」** —— `hunter-launcher --tray-menu stopp`
之前会照常开一个界面、退出码 0，脚本里看起来像是做成了，实际什么都没发生。
现在退出码 2，并列出可用的菜单项。

**`--key-file` 指的文件权限太松时不再闷声不响** —— 帮助里写着「建议 600」，
但之前只写不查。现在文件对同机其他用户可读时会提醒一句（只提醒，不中断）。

### 变化

**`.env` 写完会读回来逐项对账** —— 写之前查值里有没有换行（0.1.2 加的）拦的是
已知形状；现在写完再把文件按 `.env` 的语法解析回来，逐项比对本来要写的值，
并检查有没有同名键写了两遍。任何形式的注入或写坏都会当场报错，而不是装到一半才发现。
对账的报错**只说键名不说值**，key 与口令一个字都不会进日志。

**`~/.hunter/app/VERSION` 收成 600** —— 至此 `~/.hunter` 下由启动器写出来的文件
全部是 600、目录全部是 700，口径统一了（这个文件里只有一个版本号，不是安全问题）。

### 内部

- 镜像源候选的字段换成 `Cow`，自定义镜像源不再往 `'static` 里泄漏字符串
  （设置页每保存一次泄一点，一个进程以 KB 计）；代码库里 `Box::leak` 清零。
- CI 里给安装包改名那一步不再把版本号拼进源文件名，改成和发布流水线一样按前缀替换。
- 截图脚本多一页（额度用尽的运行面板）；GUI 回归脚本每点一下都会断言界面真的变了。

---

## [0.1.2] - 2026-09-20

迭代轮 I2 的补丁版。又做了一次全量回归（清空 `~/.hunter`、用 0.1.1 的 deb 从零装、
命令行与界面各一次、跨机真实对话跑通），修掉自审与待办池里挑出来的这几条。

### 修复

**随机生成的数据库口令可能让整套装不起来（每三次约有一两次）** —— 口令用的是标准
base64，字母表里有 `/`。上游会把它拼进 `postgresql://hunter:<口令>@postgres:5432/hunter`，
URL 的 authority 在第一个 `/` 处就收尾了，于是 api 容器一直报
`invalid integer value "…" for connection option "port"`，`up -d` 直接失败。
这是随机的 —— 上一次装好好的，这一次就不行。现在生成的口令只含字母数字。
**已经装好的机器不受影响**（口令不会被换掉）；之前装失败过的机器，
启动器现在会在日志里认出这一条并说清怎么办（最干净的办法是重装一次）。

**「复制」按钮可能什么都没复制，却照样显示「已复制」** —— `navigator.clipboard`
只在安全上下文里存在，Tauri 在 Linux 上走的是自定义协议，认不出来时那个 API 直接不存在，
于是复制静默失败、界面却说成功了。现在真复制成功才显示「已复制」，不成就显示
「复制不了，请手动选中」，并且多了一条 `document.execCommand` 的兜底路径。
更新页的安装命令与日志页的「复制全部」两处都改了。

**命令行输出接管道会崩** —— `hunter-launcher --status | head -2` 之前会打出一段
Rust backtrace，退出码 134。Rust 默认忽略 `SIGPIPE`，于是写进已关闭的管道会 panic。
命令行模式下现在恢复系统默认行为，像 `cat` 一样安静结束。

**「打开链接」的白名单还能被绕过** —— `http://localhost:1@evil.com/` 里
`localhost:1` 是 URL 语法中的 userinfo，真正的主机是 `evil.com`。I1 修的那一版
只判「主机名后面紧跟 `:`」，会把它放行。现在 authority 整段解析，里面有 `@` 一律拒绝。

**模型名 / 自定义镜像源里的换行会被原样写进 `.env`** —— `.env` 一行一个值，
多一个换行等于多定义一个环境变量，能把 `HUNTER_API_KEY` 这类关键项盖掉。
现在写 `.env` 前逐项检查换行，自定义镜像源另外要过一遍形状校验
（`主机[:端口]/路径`），不合规当场给一句人话。

**网关限流（HTTP 429）会被显示成「key 无效」** —— 那会让人去重新申请一把
根本不需要的 key。新增错误码 `E_RATE_LIMITED`，文案直说「这不是 key 的问题，
等一分钟再试」，等待秒数取自响应头 `retry-after`。

### 变化

**设置页多了「谁能打开 Hunter」** —— 可以把 Hunter 的网页端口从「所有网卡」
收紧成「只有这台电脑」。**默认没有变**，仍然是所有网卡（同一网络里的设备都能打开，
而且用的是你的额度）—— 默认值该是哪一个属于产品决策，还在等用户拍板。
改完要在运行面板点一次「重启」才生效。

**`.deb` 的版本号改用 Debian 写法** —— 预发布版从 `0.1.0-rc.2` 变成 `0.1.0~rc.2`。
dpkg 把 `-` 后面那一段当成修订号（越大越新），所以原来从 rc 升到正式版会被 apt
判成降级并拒绝。正式版的文件名不受影响。

**COS 上的旧预发布包会被清掉** —— 发布流水线每次上传完新版之后跑一遍清理：
正式版一个不删，预发布版只在同号正式版已经发布、而且不是最新的那个时才删。

**`~/.hunter` 下的子目录也收成 700** —— 之前只有根目录是 700，
`app`、`logs` 这几个跟着系统 umask 走（在 umask 是 002 的机器上是 775）。

---

## [0.1.1] - 2026-09-20

迭代轮 I1 的补丁版。全量回归（清空 `~/.hunter`、用 0.1.0 的 deb 从零装一遍、
GUI 与命令行各一次、真实对话跑通）之后，修掉自审与待办池里挑出来的这几条。

### 修复

**同一台机器上的两个工作目录会互相顶掉** —— 换过 `HUNTER_HOME` 再装一次，
`docker compose -p hunter up -d` 会把先装好的那一套**连配置带端口一起顶掉**。
现在安装 / 启动 / 升级 / 切模型 / 备份前都会先确认 `hunter` 这个项目名是不是自己的，
不是就停下来报 `E_PROJECT_CONFLICT`，并写清另一套在哪个目录、怎么切回去或者把它停掉。
数据卷从来不会被这个动作删掉，但配置与端口会，所以这一条按 P0 处理。

**"打开链接"的白名单能被绕过** —— 明文 http 只放行本机地址这一条是按前缀比的，
`http://localhost.example.com` 会被当成本机。现在要求主机名后面紧跟 `:`、`/` 或结束。

**把中文粘进"自带模型 key"那个框可能让脱敏函数崩掉** —— 打码时按字节取前三个字符，
遇到多字节字符会切在半个字上。改成按字符取。

### 变化

**`~/.hunter` 下由启动器写出来的文件统一收成 600** —— 之前只有 `.env` 是 600，
`launcher.toml`、`docker-compose.yml` 与覆盖文件是 644。里面本来就没有 key，
但口径统一更省心（升级后重新装一次或改一次设置就会自动收紧）。

**Windows 上的窗口按钮挪到右上角** —— 顺序也反成"最小化 / 最大化 / 关闭"。
视觉稿是 mac 风格的左上角，照搬到 Windows 上会让人点错。macOS 与 Linux 不变。
**这一条没有 Windows 真机验证**，只有代码路径与 CI 编译。

**最小化 / 最大化按钮有中文提示了** —— 之前悬停显示的是 `—` 和 `⤢` 两个符号。

**额度用完时，填 key 那一步就会说** —— 之前不会。额度接口在用尽时返回的是
HTTP 200（体里 `exhausted: true`），402 只出现在真正发消息的那个接口上，
所以校验一路绿灯，用户要等装完、在 Hunter 里发第一句话被拒才知道。
现在 key 仍然判为有效（它确实是好的，第二天 0 点重置，也能换自带 key），
但会明确提醒一句，命令行与界面都有。

**没有图形环境时给一段人话** —— 在没有 `DISPLAY` 的 SSH 会话里直接跑
`hunter-launcher` 会在 GTK 初始化那一步 panic，吐一段 Rust backtrace 加一个
core dump。现在直说「这台机器没有图形环境」并列出命令行的等价命令。

### 已知仍未解决

网关对四种 key 错误返回完全相同的 401、上游 api 缺四条只读接口、
`.env.example` 预填的公开字符串 —— 这三条都要上游 hunter-community 配合，
已经提了 issue（#33 / #34 / #35），启动器侧继续按"拿不到就显示 —"处理。

---

## [0.1.0] - 2026-09-20

第一个公开版本。**标记为预发布（prerelease）**：安装包没有代码签名，
Windows 会触发 SmartScreen、macOS 需要手动放行（步骤见 README 与 `docs/常见问题.md`）。
要不要买证书、转正式版由用户决定。

### 新增

**部署向导** —— 检测 Docker（Engine / Desktop / OrbStack / Colima，Podman 标为实验支持）、
校验 `hunt_tools_` key 并显示真实额度、选模型（Hunter 网关或自带 OpenAI 兼容 key）、
测速选镜像源、拉 6 个镜像（每镜像一条真实进度条）、写配置、起容器、等 6 个服务健康、开浏览器。

**端口冲突自动处理** —— 3100 / 8100 / 3921 / 5442 / 6479 被占用时自动换到空闲端口并写进配置。
除 web 外的端口一律只绑 `127.0.0.1`。

**运行面板** —— 服务状态、今日额度、数据源、容器日志、停止 / 重启 / 日志 / 反馈。

**托盘常驻** —— 11 项菜单，状态行每 15 秒刷新；退出时若容器还在跑会问"保持后台运行"还是"一起停止"。
三平台开机自启（默认关，显示的是系统里的真实状态）。

**Hunter 升级与回滚** —— 每 6 小时对比上游最新 Release tag；升级前自动 `pg_dump` 到
`~/.hunter/backups/` 并备份 `.env` 与两个 compose 文件（**数据库没备份成功就不往下走**）；
拉镜像或起容器失败时自动把配置写回旧版本并用旧镜像重启，报 `E_UPDATE_FAILED`。
界面上能看到 Release Notes 摘要，跨大版本会额外提醒。

**启动器自更新** —— 启动 20 秒后 + 每 24 小时查一次，更新清单用 minisign 签名，
公钥编译在程序里，验不过不装。AppImage / Windows / macOS 就地安装并重启；
`.deb` 因为换 `/usr` 下的文件要 root，改成"下载到 `~/.hunter/updates/` + 给一条
`sudo apt install` 命令"。**任何情况下都不自动安装。**

**离线包** —— `--export-images` 打包、界面或 `--import-images` 导入；
导入后逐个 `docker image inspect` 复核，六个齐了才跳过拉取。给内网机器用。

**命令行模式** —— `--headless` 与界面版跑的是同一套代码。另有 `--status` / `--start` /
`--stop` / `--restart` / `--down` / `--logs` / `--diagnose` / `--check-update` / `--upgrade` /
`--backups` / `--pull-only` / `--self-update`。
`--check-update` 与 `--self-update` 在没有桌面的服务器上也能用（GUI 走 Tauri 的 updater
插件，它要 AppHandle；headless 自己读清单并用同一把公钥验签）。

**反馈与诊断** —— 分节预览、逐节删减、整包脱敏自查、导出 zip、预填 GitHub issue。
**不上传任何东西**，发不发由你决定。

**遥测** —— 默认关闭，而且当前没有上报服务端。打开也只是写本机 `queue.jsonl`，
可以在设置里看原文、随时清空；关掉的那一刻队列会被删掉。

**国内源** —— 镜像与下载都走腾讯云香港，两个源真实测速后选快的，拉失败自动换源重试 3 次。

### 已知问题

* **AppImage 78.01 MiB**，远超设计目标里的 40 MB —— 要自带整套 WebKitGTK，压不下去。
  其余格式都很小：`.deb` 3.82 MiB、Windows NSIS 2.64 MiB、MSI 3.46 MiB、macOS dmg 6.51 MiB。
  介意体积就别用 AppImage。（都是 v0.1.0 实际发布产物的值。）
* **`.deb` 装的启动器不能就地自更新**（见上）。
* **运行面板「今日对话」「晨报」两张卡显示 `—`**：hunter-community 的 api 没有对应的
  免登录接口（逐条实测过）。缺哪几条在面板上可以点开看。
* **Windows / macOS 只在 CI 里编译与打包通过，没有在真机上跑过。**
* **一台机器上只能跑一套**：用 `HUNTER_HOME` 开第二个工作目录时，两套会抢同一个
  compose 项目名，后启动的会顶掉先装好的（数据卷不会丢，重装一次就回来）。
* **key 无效的四种原因分不开**：网关对"格式错 / 不存在 / 已吊销 / 没带"返回的响应
  字节级完全相同，只能统一显示"网关不认这把 key"。
* **额度用尽（402）与限流（429）没有实测过**，处理逻辑是照着网关代码写的。
* **窗口是直角的**，视觉稿里是圆角 —— 要做圆角得开窗口透明并依赖桌面合成器，
  Linux 上不是所有环境都支持，做不好会出现黑色直角。
* **Windows 上三个窗口按钮在左上角**（视觉稿是 mac 风格），与 Windows 习惯相反。
* **Podman 完全没有验证过**，虽然会被识别出来。

### 安全

* `hunt_tools_` key 只存 `~/.hunter/app/.env`（权限 600）与进程内存；
  不进日志、不进遥测、不进诊断包，界面与日志里一律打码成 `hunt_tools_****`。
* 子进程调用一律用参数数组，不拼 shell 字符串。
* 写完 compose 配置后用 `docker compose config --format json` 复核一遍
  "除 web 外的端口是否都绑在 127.0.0.1"，没绑住就拒绝启动。
* 启动器**从不**执行 `docker compose down -v`。

---

<!--
## [未发布]

### 新增
### 变更
### 修复
-->

[0.1.14]: https://github.com/agentpit-io/HunterLauncher/releases/tag/launcher-v0.1.14
[0.1.0]: https://github.com/agentpit-io/HunterLauncher/releases/tag/launcher-v0.1.0
