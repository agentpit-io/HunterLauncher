# 更新日志

本文件记录**用户看得见的**变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。时间一律上海时间。

`scripts/release-notes.sh` 会把对应版本的小节原样抄进 GitHub Release 说明，
所以这里只写变化，下载地址、未签名放行步骤那些每版都一样的内容由脚本补。

---

## [0.1.3] - 2026-09-20

迭代轮 I3 的补丁版。又做了一次全量回归（清空 `~/.hunter`、用 0.1.2 的 deb
从零装、命令行与界面各一次、真实对话跑通），修掉本轮自审与回归撞出来的几条。

### 修复

**额度用完了，运行面板上一个字都没提** —— 这是本轮回归当场撞上的：测试 key 的
今日额度在回归途中真的用完了（网关 `remaining` 为 0），Hunter 里的对话被挡住，
而启动器的运行面板只摆着「304,144 / 300,000」两个数字，进度条还是正常的琥珀金。
输入 key 那一页白纸黑字写着「额度用尽会明确提示，不会静默降级」—— 装完之后那句话
其实没有兑现。现在额度用完时面板上会有一条明确的提示（用了多少、什么时候重置、
想现在就用该怎么办），额度卡的副行写「已用完 · 明天 00:00（上海）重置」，
进度条转成警示色；`hunter-launcher --status` 也一样。

**`--key-file` 指的文件权限太松时不再闷声不响** —— 帮助里写着「建议 600」，
但之前只写不查。现在文件对同机其他用户可读时会提醒一句（只提醒，不中断）。

### 变化

**`.env` 写完会读回来逐项对账** —— 写之前查值里有没有换行（0.1.2 加的）拦的是
已知形状；现在写完再把文件按 `.env` 的语法解析回来，逐项比对本来要写的值，
并检查有没有同名键写了两遍。任何形式的注入或写坏都会当场报错，而不是装到一半才发现。
对账的报错**只说键名不说值**，key 与口令一个字都不会进日志。

**`~/.hunter/app/VERSION` 收成 600** —— 至此 `~/.hunter` 下由启动器写出来的文件
全部是 600、目录全部是 700，口径统一了（这个文件里只有一个版本号，不是安全问题）。

### 内部

- 镜像源候选的字段换成 `Cow`，自定义镜像源不再往 `'static` 里泄漏字符串
  （设置页每保存一次泄一点，一个进程以 KB 计）；代码库里 `Box::leak` 清零。
- CI 里给安装包改名那一步不再把版本号拼进源文件名，改成和发布流水线一样按前缀替换。
- 截图脚本多一页（额度用尽的运行面板）；GUI 回归脚本每点一下都会断言界面真的变了。

---

## [0.1.2] - 2026-09-20

迭代轮 I2 的补丁版。又做了一次全量回归（清空 `~/.hunter`、用 0.1.1 的 deb 从零装、
命令行与界面各一次、跨机真实对话跑通），修掉自审与待办池里挑出来的这几条。

### 修复

**随机生成的数据库口令可能让整套装不起来（每三次约有一两次）** —— 口令用的是标准
base64，字母表里有 `/`。上游会把它拼进 `postgresql://hunter:<口令>@postgres:5432/hunter`，
URL 的 authority 在第一个 `/` 处就收尾了，于是 api 容器一直报
`invalid integer value "…" for connection option "port"`，`up -d` 直接失败。
这是随机的 —— 上一次装好好的，这一次就不行。现在生成的口令只含字母数字。
**已经装好的机器不受影响**（口令不会被换掉）；之前装失败过的机器，
启动器现在会在日志里认出这一条并说清怎么办（最干净的办法是重装一次）。

**「复制」按钮可能什么都没复制，却照样显示「已复制」** —— `navigator.clipboard`
只在安全上下文里存在，Tauri 在 Linux 上走的是自定义协议，认不出来时那个 API 直接不存在，
于是复制静默失败、界面却说成功了。现在真复制成功才显示「已复制」，不成就显示
「复制不了，请手动选中」，并且多了一条 `document.execCommand` 的兜底路径。
更新页的安装命令与日志页的「复制全部」两处都改了。

**命令行输出接管道会崩** —— `hunter-launcher --status | head -2` 之前会打出一段
Rust backtrace，退出码 134。Rust 默认忽略 `SIGPIPE`，于是写进已关闭的管道会 panic。
命令行模式下现在恢复系统默认行为，像 `cat` 一样安静结束。

**「打开链接」的白名单还能被绕过** —— `http://localhost:1@evil.com/` 里
`localhost:1` 是 URL 语法中的 userinfo，真正的主机是 `evil.com`。I1 修的那一版
只判「主机名后面紧跟 `:`」，会把它放行。现在 authority 整段解析，里面有 `@` 一律拒绝。

**模型名 / 自定义镜像源里的换行会被原样写进 `.env`** —— `.env` 一行一个值，
多一个换行等于多定义一个环境变量，能把 `HUNTER_API_KEY` 这类关键项盖掉。
现在写 `.env` 前逐项检查换行，自定义镜像源另外要过一遍形状校验
（`主机[:端口]/路径`），不合规当场给一句人话。

**网关限流（HTTP 429）会被显示成「key 无效」** —— 那会让人去重新申请一把
根本不需要的 key。新增错误码 `E_RATE_LIMITED`，文案直说「这不是 key 的问题，
等一分钟再试」，等待秒数取自响应头 `retry-after`。

### 变化

**设置页多了「谁能打开 Hunter」** —— 可以把 Hunter 的网页端口从「所有网卡」
收紧成「只有这台电脑」。**默认没有变**，仍然是所有网卡（同一网络里的设备都能打开，
而且用的是你的额度）—— 默认值该是哪一个属于产品决策，还在等用户拍板。
改完要在运行面板点一次「重启」才生效。

**`.deb` 的版本号改用 Debian 写法** —— 预发布版从 `0.1.0-rc.2` 变成 `0.1.0~rc.2`。
dpkg 把 `-` 后面那一段当成修订号（越大越新），所以原来从 rc 升到正式版会被 apt
判成降级并拒绝。正式版的文件名不受影响。

**COS 上的旧预发布包会被清掉** —— 发布流水线每次上传完新版之后跑一遍清理：
正式版一个不删，预发布版只在同号正式版已经发布、而且不是最新的那个时才删。

**`~/.hunter` 下的子目录也收成 700** —— 之前只有根目录是 700，
`app`、`logs` 这几个跟着系统 umask 走（在 umask 是 002 的机器上是 775）。

---

## [0.1.1] - 2026-09-20

迭代轮 I1 的补丁版。全量回归（清空 `~/.hunter`、用 0.1.0 的 deb 从零装一遍、
GUI 与命令行各一次、真实对话跑通）之后，修掉自审与待办池里挑出来的这几条。

### 修复

**同一台机器上的两个工作目录会互相顶掉** —— 换过 `HUNTER_HOME` 再装一次，
`docker compose -p hunter up -d` 会把先装好的那一套**连配置带端口一起顶掉**。
现在安装 / 启动 / 升级 / 切模型 / 备份前都会先确认 `hunter` 这个项目名是不是自己的，
不是就停下来报 `E_PROJECT_CONFLICT`，并写清另一套在哪个目录、怎么切回去或者把它停掉。
数据卷从来不会被这个动作删掉，但配置与端口会，所以这一条按 P0 处理。

**"打开链接"的白名单能被绕过** —— 明文 http 只放行本机地址这一条是按前缀比的，
`http://localhost.example.com` 会被当成本机。现在要求主机名后面紧跟 `:`、`/` 或结束。

**把中文粘进"自带模型 key"那个框可能让脱敏函数崩掉** —— 打码时按字节取前三个字符，
遇到多字节字符会切在半个字上。改成按字符取。

### 变化

**`~/.hunter` 下由启动器写出来的文件统一收成 600** —— 之前只有 `.env` 是 600，
`launcher.toml`、`docker-compose.yml` 与覆盖文件是 644。里面本来就没有 key，
但口径统一更省心（升级后重新装一次或改一次设置就会自动收紧）。

**Windows 上的窗口按钮挪到右上角** —— 顺序也反成"最小化 / 最大化 / 关闭"。
视觉稿是 mac 风格的左上角，照搬到 Windows 上会让人点错。macOS 与 Linux 不变。
**这一条没有 Windows 真机验证**，只有代码路径与 CI 编译。

**最小化 / 最大化按钮有中文提示了** —— 之前悬停显示的是 `—` 和 `⤢` 两个符号。

**额度用完时，填 key 那一步就会说** —— 之前不会。额度接口在用尽时返回的是
HTTP 200（体里 `exhausted: true`），402 只出现在真正发消息的那个接口上，
所以校验一路绿灯，用户要等装完、在 Hunter 里发第一句话被拒才知道。
现在 key 仍然判为有效（它确实是好的，第二天 0 点重置，也能换自带 key），
但会明确提醒一句，命令行与界面都有。

**没有图形环境时给一段人话** —— 在没有 `DISPLAY` 的 SSH 会话里直接跑
`hunter-launcher` 会在 GTK 初始化那一步 panic，吐一段 Rust backtrace 加一个
core dump。现在直说「这台机器没有图形环境」并列出命令行的等价命令。

### 已知仍未解决

网关对四种 key 错误返回完全相同的 401、上游 api 缺四条只读接口、
`.env.example` 预填的公开字符串 —— 这三条都要上游 hunter-community 配合，
已经提了 issue（#33 / #34 / #35），启动器侧继续按"拿不到就显示 —"处理。

---

## [0.1.0] - 2026-09-20

第一个公开版本。**标记为预发布（prerelease）**：安装包没有代码签名，
Windows 会触发 SmartScreen、macOS 需要手动放行（步骤见 README 与 `docs/常见问题.md`）。
要不要买证书、转正式版由用户决定。

### 新增

**部署向导** —— 检测 Docker（Engine / Desktop / OrbStack / Colima，Podman 标为实验支持）、
校验 `hunt_tools_` key 并显示真实额度、选模型（Hunter 网关或自带 OpenAI 兼容 key）、
测速选镜像源、拉 6 个镜像（每镜像一条真实进度条）、写配置、起容器、等 6 个服务健康、开浏览器。

**端口冲突自动处理** —— 3100 / 8100 / 3921 / 5442 / 6479 被占用时自动换到空闲端口并写进配置。
除 web 外的端口一律只绑 `127.0.0.1`。

**运行面板** —— 服务状态、今日额度、数据源、容器日志、停止 / 重启 / 日志 / 反馈。

**托盘常驻** —— 11 项菜单，状态行每 15 秒刷新；退出时若容器还在跑会问"保持后台运行"还是"一起停止"。
三平台开机自启（默认关，显示的是系统里的真实状态）。

**Hunter 升级与回滚** —— 每 6 小时对比上游最新 Release tag；升级前自动 `pg_dump` 到
`~/.hunter/backups/` 并备份 `.env` 与两个 compose 文件（**数据库没备份成功就不往下走**）；
拉镜像或起容器失败时自动把配置写回旧版本并用旧镜像重启，报 `E_UPDATE_FAILED`。
界面上能看到 Release Notes 摘要，跨大版本会额外提醒。

**启动器自更新** —— 启动 20 秒后 + 每 24 小时查一次，更新清单用 minisign 签名，
公钥编译在程序里，验不过不装。AppImage / Windows / macOS 就地安装并重启；
`.deb` 因为换 `/usr` 下的文件要 root，改成"下载到 `~/.hunter/updates/` + 给一条
`sudo apt install` 命令"。**任何情况下都不自动安装。**

**离线包** —— `--export-images` 打包、界面或 `--import-images` 导入；
导入后逐个 `docker image inspect` 复核，六个齐了才跳过拉取。给内网机器用。

**命令行模式** —— `--headless` 与界面版跑的是同一套代码。另有 `--status` / `--start` /
`--stop` / `--restart` / `--down` / `--logs` / `--diagnose` / `--check-update` / `--upgrade` /
`--backups` / `--pull-only` / `--self-update`。
`--check-update` 与 `--self-update` 在没有桌面的服务器上也能用（GUI 走 Tauri 的 updater
插件，它要 AppHandle；headless 自己读清单并用同一把公钥验签）。

**反馈与诊断** —— 分节预览、逐节删减、整包脱敏自查、导出 zip、预填 GitHub issue。
**不上传任何东西**，发不发由你决定。

**遥测** —— 默认关闭，而且当前没有上报服务端。打开也只是写本机 `queue.jsonl`，
可以在设置里看原文、随时清空；关掉的那一刻队列会被删掉。

**国内源** —— 镜像与下载都走腾讯云香港，两个源真实测速后选快的，拉失败自动换源重试 3 次。

### 已知问题

* **AppImage 78.01 MiB**，远超设计目标里的 40 MB —— 要自带整套 WebKitGTK，压不下去。
  其余格式都很小：`.deb` 3.82 MiB、Windows NSIS 2.64 MiB、MSI 3.46 MiB、macOS dmg 6.51 MiB。
  介意体积就别用 AppImage。（都是 v0.1.0 实际发布产物的值。）
* **`.deb` 装的启动器不能就地自更新**（见上）。
* **运行面板「今日对话」「晨报」两张卡显示 `—`**：hunter-community 的 api 没有对应的
  免登录接口（逐条实测过）。缺哪几条在面板上可以点开看。
* **Windows / macOS 只在 CI 里编译与打包通过，没有在真机上跑过。**
* **一台机器上只能跑一套**：用 `HUNTER_HOME` 开第二个工作目录时，两套会抢同一个
  compose 项目名，后启动的会顶掉先装好的（数据卷不会丢，重装一次就回来）。
* **key 无效的四种原因分不开**：网关对"格式错 / 不存在 / 已吊销 / 没带"返回的响应
  字节级完全相同，只能统一显示"网关不认这把 key"。
* **额度用尽（402）与限流（429）没有实测过**，处理逻辑是照着网关代码写的。
* **窗口是直角的**，视觉稿里是圆角 —— 要做圆角得开窗口透明并依赖桌面合成器，
  Linux 上不是所有环境都支持，做不好会出现黑色直角。
* **Windows 上三个窗口按钮在左上角**（视觉稿是 mac 风格），与 Windows 习惯相反。
* **Podman 完全没有验证过**，虽然会被识别出来。

### 安全

* `hunt_tools_` key 只存 `~/.hunter/app/.env`（权限 600）与进程内存；
  不进日志、不进遥测、不进诊断包，界面与日志里一律打码成 `hunt_tools_****`。
* 子进程调用一律用参数数组，不拼 shell 字符串。
* 写完 compose 配置后用 `docker compose config --format json` 复核一遍
  "除 web 外的端口是否都绑在 127.0.0.1"，没绑住就拒绝启动。
* 启动器**从不**执行 `docker compose down -v`。

---

<!--
## [未发布]

### 新增
### 变更
### 修复
-->

[0.1.0]: https://github.com/agentpit-io/HunterLauncher/releases/tag/launcher-v0.1.0
